# Diagnostic qualification from 782e141

The single diagnostic candidate was v3.1.67, sequence 146, from
`782e141832ed662a7f9ffb7f0bd38f09ccc888e2`, on Ubuntu 24.04 at
107.175.53.219. [Workflow 34671498302](https://github.com/albertloky/SBXR/actions/runs/34671498302)
passed both native builds and draft/signing verification, then failed acceptance.
No stable release or replacement candidate was created. Implementation and harness
bytes stayed fixed throughout live diagnostics.

## Findings

- **Command mistake — incomplete evidence retention.** Clean installation, setup,
  real outside-runner proxy traffic, and complete removal succeeded. The operator
  omitted the existing known-secret inventory capture before removal. The private
  capture-review helper then refused the absent `24-known-secrets.json`. The
  original exception and receipts were preserved; validated failure facts were
  submitted through the actual SSH submission command. No pass was submitted.
- **Harness defect — lost refusal details.** During separate public-operation
  diagnostics, pre-activation interruption reported `boundary-observed` and
  `descendants_reaped=true`, but Finish cleanup returned
  `PROXY-INSTALLATION-ACTION-REFUSED`. The menu driver captured the product output
  and returned nonzero. `run_action` returned before scanning or retaining that
  output, so both direct callers and the operator's diagnostic wrapper lost the
  detailed safety check and correction. This also affected failure-cleanup logs.
- **Unresolved cause — transient Finish cleanup refusal.** Public inspection
  showed Setup incomplete at Configuration validated, with the service disabled
  and inactive. The one targeted supported Finish cleanup reproduction succeeded.
  The original detailed refusal was lost by the confirmed harness defect, so no
  product cause is asserted and no speculative product change is made.
- **Preparation mistakes, corrected before live execution.** An initially chosen
  sequence and noncanonical JSON encoding were refused before dispatch. Operator
  staging was also removed before signing because it had been prepared too early.
  These did not create extra candidates or qualify as product defects.

## Diagnostic coverage, not acceptance

The collector accepted **zero scenarios**. Its original qualification remains
failed and its request was replaced by its own STOP marker. After supported
product cleanup and verified transport removal, the exact original transport was
restaged for independent diagnostic delivery using the existing up/down commands.
No fabricated request or accepted prefix was used.

| Scope | Observed result |
| --- | --- |
| 01 clean install/setup/outside traffic/removal | Product operations passed; evidence submission recorded failure |
| 02 foreign configuration refusal | Public refusal and unchanged inventory passed in the independent helper |
| 03 pre-commit interruption/cleanup | Interruption and descendant cleanup passed; first cleanup refused; one targeted cleanup succeeded |
| 04 post-commit interruption/recovery | Independent public recovery and descendant cleanup passed; not full outside-witness scenario acceptance |
| 05 metadata drift | Refusal and unchanged inventory passed; original mode restored |
| 06 removal interruption | Interruption, descendant cleanup, Finish removal and final absence passed |
| 07–18 | Blocked as collector scenarios; not exercised independently |
| 19 Check/Update/Recover | Independent public menu checks passed with unchanged inventory; declared refreshed-package scenario not accepted |
| 20–24 | Blocked; not exercised independently |
| 25 Karing | Untested; no fresh client acceptance evidence and no connection/settings changes |

## Consolidated repair

The shared action helper now clears stale output, scans captured stdout even when
its real menu driver fails, retains only safely scanned output, and reports the
existing allowlisted diagnostic lines while preserving failure status. The
operator wrapper delegates reporting to that helper so direct callers and wrapped
callers get the same details once. Existing success-result assertions remain.
No collector continuation, new evidence format, policy gate, or product refactor
is part of this repair.

## Cleanup and validation

Live diagnostics ended at 04:23:31 UTC, about 40 minutes after starting. Supported
removal and transport cleanup were verified before editing. Certbot was restored
to revision 5893 (5.8.0); snapd 27738 and core24 2124 were unchanged. Snap holds were
removed, APT timers and the official renewal timer were active/enabled, and no new
certificate issuance occurred. Product paths, temporary transport route/CA/unit,
staged operator/evidence directories, test processes and test listeners were absent.
The original SSH controls remained alive. Raw failure and diagnostic captures are
retained privately; workflow failure artifacts preserve the original failed run.

Validation passed:

- The new real-menu regression failed before the fix and passed afterward: three
  tests cover both direct and wrapped callers, safe refusal retention/reporting,
  stale-output clearing, secret rejection, quiet success, and actual menu inputs.
- Applicable packaged-action and cleanup Go tests passed.
- One Ubuntu fixture rehearsal passed all 20 groups, including actual process
  tracing, systemd, locks, deadlines, and descendant cleanup. Its helper suite ran
  304 tests with one platform-dependent skip.
- `check-readiness.py` accepted the report and all original logs. Its local suite
  ran 304 tests with 18 Linux-dependent skips; entry-point rehearsal passed.
- The unchanged real-SSH boundary suite's prior 11 passing cases and forced-parent-
  death cleanup result were reused. This diagnostic run additionally exercised
  actual staged outside disclosure and failure-evidence submission over SSH.
- Final fixture cleanup and the restored-host check passed at 04:30:48 UTC.

The confirmed harness defect is repaired and regression-tested. The transient
product refusal remains unresolved; subscription scenarios and Karing remain
untested. A fresh qualification is justified to validate the repaired harness and
collect the remaining acceptance evidence, subject to explicit user instruction.
No replacement candidate was created or qualified after repair.
