# v3.1.69 live qualification — 2026-09-12

One candidate was qualified on `107.175.53.219`: `v3.1.69`, sequence 148,
from commit `f83b338b251ad174c57923875aa8d402f187c0a8`, in
[workflow 34694246425](https://github.com/albertloky/SBXR/actions/runs/34694246425).
The workflow failed. The candidate remains a failed prerelease; no stable
release or replacement candidate was created. Uncommitted navigation cleanup
was excluded and preserved.

## Observed results

| Scenarios | Result |
| --- | --- |
| 01 baseline clean | Collector accepted installation, setup, actual outside proxy traffic, and Complete removal. |
| 02 baseline refusal | Collector accepted foreign-file detection, setup refusal without mutation, and fixture-only restoration. |
| 03 baseline precommit | Failed after the actual interruption boundary was observed and all descendants were reaped. `Finish cleanup` refused with `PROXY-INSTALLATION-ACTION-REFUSED`, identifying `Prepared Action facts` as the failed safety check. |
| 04–25 | Blocked after the failure; not run and not counted as passes. |

The original refusal was:

```text
INTERRUPTION_RESULT reason=boundary-observed descendants_reaped=true
SBXR_MENU_SESSION_REFUSED phase=result-mismatch code=PROXY-INSTALLATION-ACTION-REFUSED
Failed safety check: Prepared Action facts
Correction: Review Finish cleanup again after restoring every changed authority fact.
```

The strict primary SSH control exited after that refusal. The secondary
control remained available. No test was retried. A separate invocation of the
existing supported failure-cleanup routine reviewed the current state, completed
`Finish cleanup`, then completed removal and proved `Not installed`. Its success
does not change the failed qualification result.

The public-action refusal is confirmed. Which prepared fact changed, and whether
the cause is a product defect or an interaction with the harness, remains
unresolved. No implementation or harness repair was attempted. The next useful
step is a focused investigation of this refusal using the retained state and
captures before preparing another candidate.

## Validation and preparation limits

Both native architecture jobs passed their ordinary tests, race tests, vet,
dependency verification, and package checks. The valid Ubuntu fixture report
from the preceding repair was rechecked against this exact source; readiness
passed with 307 local helper tests and 18 platform skips. The signed manifest,
decision chain, four draft assets, both native asset declarations, and staged
136-file operator bundle were verified. These checks did not predict the live
scenario-03 refusal.

Preparation had two command mistakes, corrected before the live clock started:
the boundary artifact was initially assumed to contain all package files, and
tar created the fixture subdirectory with mode 0755 instead of 0700. The actual
draft assets were downloaded and verified; the directory permission was
corrected and unchanged staging checks passed. After the collector stopped,
an archive-check command incorrectly expected an active JSON request and a
remote decision file. The collector's actual `STOP` sentinel and exact submitted
failure result were verified instead; its decision was retrieved from the
workflow artifact. None of these corrections changed candidate or harness code.

The Owner reported fresh Karing latency of 177 ms on the unchanged selected
server. Automation returned `noWindowsAvailable`. That report establishes
readiness only; no Karing qualification checks were completed. Existing Karing
settings and the connection were not changed by this run.

## Verified cleanup and retained evidence

Final VPS checks passed at `2026-09-12T13:14:13Z`:

- SBXR, owned proxy state, test transport, listeners, units, cgroups, firewall
  rules, certificate lineage, staging directories, and the unused lock file
  were absent.
- Certbot revision 5893, snapd 27738, and core24 2124 were restored. Temporary
  snap holds were removed; APT and official renewal timers were active/enabled.
- No new certificate issuance occurred: the historical count remained seven.
- SSH remained healthy. The unrelated older preflight directory was preserved.

Original failure output, provisional-secret capture, pre-cleanup raw VPS
archive, collector stop/result archive, supported cleanup evidence, workflow
failure decision, accepted scenario receipts, and host verification are retained
privately under
`/Users/Albert/Documents/Codex/SBXR/.scratch/acceptance/sbxr-live-f83b338-op5vdpr0`.

Qualification acceptance is **2 accepted, 1 failed, 22 blocked**. Further live
qualification should wait until the refusal is understood and the Owner
explicitly requests another run.
