# v3.1.70 MVP live qualification — incomplete, recorded as failed

- Candidate: `v3.1.70`, sequence `149`, commit `b14a4cfd92f6324ed26487d7bde9a47a25142789`.
- [Workflow 34701174649](https://github.com/albertloky/SBXR/actions/runs/34701174649).
- Host: `107.175.53.219`, Ubuntu Server 24.04 `amd64`.
- Current scope: `mvp-live-v1`, the five normal product journeys. Retired V4 tests were not run.
- Preparation began around 14:58UTC; first live request 15:27:23UTC; final VPS cleanup verified 15:56:33UTC on 2026-09-12.
- Overall result: incomplete qualification. GitHub recorded a failed prerelease and completed `cleanup-unqualified` successfully. No stable release, repair, or replacement candidate was produced.

## Results

| Journey | Result | Observations |
|---|---|---|
| `mvp-install` | Accepted by collector | Exact packaged install; reviewed Start setup; status/details; Check already-current; no recovery available; actual outside proxy traffic whose egress matched the VPS and differed from the Mac direct route; SSH preserved. Collector advanced to the next journey at 15:30:05UTC. |
| `mvp-subscription` | Partial; not accepted | Enable subscription completed 15:31:04UTC. Normal outside TLS validation, exactly one matching VLESS/REALITY node, wrong-token404/no-store, protected file metadata, and checked service-log/process-argument secret containment passed. Karing import, fresh test-node latency, manual refresh, and selected-connection acceptance were not observed. |
| `mvp-credentials` | Not run | Dependent on the incomplete subscription journey. |
| `mvp-renewal` | Not run | Dependent journey; certificate replacement was not live-tested. |
| `mvp-removal` | Not run as qualification | Supported Complete removal subsequently succeeded as safety cleanup. That does not satisfy the whole restart/removal journey. |

## Why this attempt stopped

Karing automation returned `noWindowsAvailable`. The Owner initially agreed to assist, but no manual import/latency/refresh observations arrived during approximately 20minutes waiting after the test link was supplied. The operator ended the attempt as incomplete, submitted a validated `evidence-refused` failure through the existing collector interface, and verified its STOP state. This was not expiry of the collector's two-hour subscription deadline and is not an observed product defect. No assumed Karing behavior was submitted.

The exact original failure and decision downloaded from GitHub match the retained local submission. Existing Karing connection/settings were not changed by the operator. Whether the Owner independently added a temporary profile is unconfirmed; removal of `SBXR-MVP-v3.1.70`, if present, still needs Owner confirmation. Its VPS access and temporary local link file have been removed.

## Additional findings and command corrections

- **Client-evidence blocker:** automated Karing control was unavailable; no manual result was received. No conclusion about Karing compatibility follows.
- **Procedure mismatch, code review only:** the fresh-install flow requires `certificate-replaced`, but the supported managed renewal service runs ordinary Certbot renewal without a force option. A healthy fresh certificate can produce an accepted no-op. The product's forced replacement is the reviewed Repair subscription action, offered only for diagnosed failed/abandoned renewal evidence. This needs a supported procedure/product decision before expecting deterministic same-session replacement. No forced command, private role, protected configuration edit, or induced fault was used. See `internal/proxyinstallation/adapter/host/renewal.go` and `internal/proxyinstallation/repair.go` alongside the MVP procedure. This finding was not live-reproduced in this run.
- **Operator command mistake:** an initial direct curl used the system resolver and timed out through the existing Karing route. Direct pinned DNS plus physical-interface binding passed before live testing.
- **Operator command mistake:** the first local failure-envelope validation rejected Python's default ASCII escaping of nested canonical documents. UTF8 serialization corrected the command; unchanged factual content validated and was submitted. Product and collector source were unchanged.

## Validation

- Current commit Verify passed on native Ubuntu `amd64` and `arm64` (run 34700745103).
- Both candidate native builds passed ordinary and race tests, vet, module verification, and packaged native checks.
- Local root/release-tool tests passed; root tests include current MVP assembler/collector regression coverage.
- Three draft decisions reproduced with the current validator; all four authenticated draft assets matched; signed-manifest attestation and boundary reproduction passed.
- Actual SSH stdin/stdout and a physical-interface outside network route passed.
- Reused the previously verified, unchanged official sing-box 1.13.19 Darwin ARM64 client after checking its bytes against the retained package receipt.

## Verified cleanup

At15:56:33UTC:

- Reviewed Complete removal returned `SOFTWARE-LIFECYCLE-COMPLETE-REMOVAL-COMPLETED`; packaged final absence passed.
- SBXR, sing-box package/account, owned subscription/certificate resources, test processes/listeners, collector staging, unused empty mutation lock, and temporary transport CA/hosts/firewall resources were absent.
- Certbot revision 5893, snapd 27738, and core24 2124 matched the original host. No operator package downgrade or snap-refresh exercise was performed.
- `apt-daily.timer`, `apt-daily-upgrade.timer`, and `snap.certbot.renew.timer` were active and enabled. SSH was active.
- Unrelated `/root/sbxr-357-certbot-preflight` remained.
- Old outside proxy and subscription access failed. Only the recorded local test client was stopped; its listener, copied client, temporary configurations, subscription responses, and link files were removed. Redacted menu evidence was retained.
- GitHub's failed-candidate cleanup completed. The candidate is a failed prerelease, not stable.

## Handoff

Private evidence: `/Users/Albert/Documents/Codex/SBXR/.scratch/acceptance/sbxr-mvp-b14a4cf-1wjfa132` (`REPORT.md`, `HANDOFF.md`, original observation/failure documents, redacted menu captures, workflow diagnostics, and `final-vps.json`).

No source files changed; unrelated untracked files were preserved. Before another qualification, arrange active Karing assistance and resolve the supported renewal/replacement procedure. This attempt does not justify release acceptance. No replacement candidate was created.
