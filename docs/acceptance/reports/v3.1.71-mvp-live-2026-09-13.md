# v3.1.71 MVP live qualification — failed at renewal

One candidate, **v3.1.71**, sequence **150**, commit
`b14a4cfd92f6324ed26487d7bde9a47a25142789`, was tested on
`107.175.53.219` (Ubuntu Server 24.04 amd64). The implementation and harness
remained unchanged throughout. Workflow:
<https://github.com/albertloky/SBXR/actions/runs/34736064273>.

This attempt used the current five-journey `mvp-live-v1` procedure. It did not
run or accept the historical 25-scenario matrix. No stable release or replacement
candidate was created. The workflow finished with failure, and
`cleanup-unqualified` succeeded. The candidate remains a prerelease.

## Results

| Journey | Result | Evidence |
|---|---|---|
| `mvp-install` | Accepted by collector | Exact packaged installation, reviewed setup, running status/details, Check already-current, no recovery available, actual outside proxy traffic with VPS egress distinct from the Mac's direct route, and SSH access. Next request issued at 04:02:28 UTC. |
| `mvp-subscription` | Accepted by collector | Supported HTTPS enablement; trusted outside TLS; exactly one matching node; wrong-token HTTP 404/no-store; protected files and checked log/process output; actual Karing import, fresh 168 ms latency, and user-reported manual refresh success. Home UI and user confirmation preserved the original server, rules mode, and TUN setting. Next request issued at 04:17:12 UTC. |
| `mvp-credentials` | Accepted by collector | One proxy identity rotation terminated the existing outside TLS connection and refused a new connection using the old identity. The unchanged URL returned the replacement identity, and replacement proxy traffic worked. One link rotation returned old-link 404/new-link 200 with trusted TLS and unchanged proxy identity. The Owner used the new link in Karing and reported a fresh 168 ms result, also visible in the UI. Next request issued at 04:30:13 UTC. |
| `mvp-renewal` | Incomplete; failure submitted | The supported renewal service completed with exit 0 and recorded `owned_outcome: no-op`. The certificate and lineage were unchanged. Outside trusted TLS and proxy traffic remained usable, but replacement and activation of a replacement were not proved. |
| `mvp-removal` | Not run as qualification | Blocked after the renewal failure. Reviewed Complete removal subsequently passed as safety cleanup; the ordinary restart portion of this journey was not exercised. |

## Renewal finding

At 04:30:53 UTC the operator invoked `systemctl start
snap.certbot.renew.service` once. Its actual managed ExecStart was
`/usr/local/bin/sbxr --certbot-recorder`. The service completed at 04:30:57 UTC
with `Result=success` and `ExecMainStatus=0`.

The durable recorder reported invocation `snap-certbot-renew-v1`, completion
exit code 0, outcome `no-op`, and the same `cert1.pem` lineage before and after.
Independent TLS connections bound to the Mac's physical interface observed the
same certificate before and after; its expiry is September 19, 2026.
Fresh traffic through the replacement proxy identity still exited at the VPS.

This establishes a **qualification procedure mismatch**, not a demonstrated
proxy or HTTPS outage: the current fresh-install checklist requires replacement,
while the supported invocation made no replacement. The retained service journal
contains successful service lifecycle messages, not Certbot's detailed reason
for making no change. A newly issued certificate is relevant context, but the
exact internal skip reason is not established by this run.

Code review shows the managed recorder invokes ordinary snap Certbot renewal
without a force option. The reviewed Repair subscription path is conditional on
diagnosed renewal trouble, which was not present. No forced Certbot command,
private hook, protected configuration edit, induced fault, timeout extension, or
second renewal attempt was used.

At 04:31:51 UTC, the current validator accepted an `evidence-refused` failure for
`mvp-renewal` / `operation-4`, with host state `Running`, test mutations stopped,
and failed-candidate handling required. This was not a timeout. The existing
submission interface delivered the original failure on SSH stdin, and the
collector entered `STOP`. Downloaded workflow failure facts match the submitted
bytes; the downloaded decision matches the local validated decision. No
all-observed renewal record was submitted.

## Karing evidence and limits

The Owner initially reported difficulty using the replacement link and inability
to edit the old link, then explicitly corrected the result: the new link worked
at 168 ms. The operator retained both reports and the actual one-node/168 ms UI
observation. This proves replacement-link use and fresh node latency in the
current Karing session. It does **not** prove an in-place profile URL edit.

Native UI reading worked, while automated coordinate navigation still returned
`noWindowsAvailable`. Manual actions were necessary. No assumed Karing behavior
was submitted. The Owner removed the temporary test profile(s) at the end; the
final UI showed only the original `80.251.213.129` profile and visibly selected
`vm-argo-BWH.localdomain`.

## Validation and unchanged work

- Both fresh candidate native builds passed ordinary and race tests, vet,
  module verification, and packaged native checks.
- Previously valid local/current-commit validation was reused for unchanged
  components. No product or harness source was edited, so no new repair tests
  or historical V4 fixture rehearsal were introduced.
- The current validator reproduced the three draft decisions and the signed
  manifest from its boundary facts; authenticated candidate assets and signed
  attestation verification passed.
- Real packaged menus, SSH transport/stdin submission, outside TCP/TLS/proxy
  traffic, and a persistent connection were exercised. The persistent test
  connection completed 28 requests before `RemoteDisconnected` during identity
  rotation; the probe did not reconnect silently.
- A GitHub status request briefly failed with a network reset. A later status
  read succeeded. This affected reporting only, not the live scenario outcome.

## Verified cleanup

At **04:34:40 UTC**:

- Supported Complete removal had returned acceptance and `Final absence:
  Verified`.
- SBXR, sing-box package/account, subscription services, owned certificate
  lineage/hooks, managed renewal drop-in, test processes/listeners, and the
  temporary qualification service/CA/hosts/firewall/staging were absent.
- The unused empty mutation lock was removed after verifying its ownership,
  metadata, and lack of users. Only explicitly identified test files were
  removed from staging.
- SSH remained active. Certbot revision 5893, snapd 27738, and core24 2124
  matched the starting host. `apt-daily.timer`, `apt-daily-upgrade.timer`, and
  `snap.certbot.renew.timer` were active and enabled. Unrelated
  `/root/sbxr-357-certbot-preflight` remained.
- Both old and replacement outside proxy access failed, and both subscription
  links were unreachable, before the exact recorded local test clients were
  stopped. Their local listeners were then verified absent.
- Temporary local client copies, configurations, credential links, curl
  configurations, and subscription bodies were removed. Secret-bearing menu
  captures were replaced with redacted copies; retained text was scanned for
  the known test credentials.
- Karing test-profile removal and preservation of the original selected server
  were confirmed by the Owner and the actual UI.

## Handoff

Private evidence directory:
`/Users/Albert/Documents/Codex/SBXR/.scratch/acceptance/sbxr-mvp-retry-b14a4cf-ji3ju1r7`.
It contains the three submitted observations, corresponding next requests,
redacted menu captures, connection results, renewal receipt/certificate
observations, original failure and decision, downloaded workflow diagnostics,
`final-vps.json`, and local/Karing cleanup receipts.

No implementation or harness files changed; unrelated untracked files were
preserved. This report is the only new repository file from the run and is left
uncommitted. There is no successful qualification Acceptance Record.

Another unchanged qualification is not justified yet. First resolve how the
supported fresh-install procedure is meant to obtain actual certificate
replacement, and validate that execution boundary before another candidate.
Replacement activation, the complete restart/removal journey, incoming upgrades,
natural timer firing, automatic client refresh, and the omitted historical live
variants remain untested here. No repair or replacement candidate was started.
