# v3.1.72 MVP live qualification — 2026-09-13

**Result: failed; no successful Acceptance Record.** One candidate was tested from commit `b97b56859e32d10b0a303ad2a98d7734340ff0eb`, tag `v3.1.72`, sequence `151`, on Ubuntu 24.04 VPS `107.175.53.219`. The implementation and harness remained unchanged. This attempt used the five current `mvp-live-v1` journeys, not the retired 25-scenario matrix.

[Workflow 34756945063](https://github.com/albertloky/SBXR/actions/runs/34756945063) · [local evidence](../../../.scratch/acceptance/l72/)

| Journey | Result | Observed evidence |
|---|---|---|
| `mvp-install` | Accepted by collector | Exact packaged candidate; reviewed setup; useful status/details; Check already current; no recovery available; real outside proxy traffic; SSH preserved. |
| `mvp-subscription` | Accepted by collector | Trusted outside HTTPS; correct one-node artifact; wrong-token 404; protected files and checked logs/arguments; Owner-assisted Karing import, 167 ms latency and successful manual refresh; original selection/settings preserved. |
| `mvp-credentials` | Accepted by collector | One UUID rotation terminated an established connection and refused the old credential; replacement traffic worked; unchanged URL served the new identity. One URL rotation refused the old link and preserved the proxy identity. Karing displayed 172 ms; Owner confirmed replacement refresh succeeded and settings remained unchanged. |
| `mvp-renewal` | Failed | One reviewed Replace subscription certificate action reached managed Certbot, which returned an explicit production CA rate-limit rejection. Certificate remained unchanged. |
| `mvp-removal` | Not run as an acceptance journey | Testing stopped after the renewal failure. Supported Complete removal subsequently passed as safety cleanup; the restart portion was not exercised. |

## Confirmed cause and findings

At `2026-09-13T13:22:26Z`, Let's Encrypt returned `urn:ietf:params:acme:error:rateLimited`: five certificates had already been issued for the exact identifier set within the preceding 168 hours. The response's retry time was `2026-09-14 16:16:25 UTC` — **September 15 at 00:16:25 Hong Kong time**. The renewal recorder retained exit code 1 and the unchanged lineage. A fresh outside connection still trusted the original certificate and confirmed that its identity had not changed.

This is a confirmed external issuance-limit blocker, not evidence of an HTTP-01 reachability failure. The menu reported `PROXY-INSTALLATION-SUBSCRIPTION-CHANGE-INCOMPLETE`. Its generic correction mentioned TCP 80, the provider firewall, or lineage correction, but did not explain the CA limit; that diagnostic limitation remains for a separate change. Successful live replacement and activation remain unvalidated. No second issuance, Finish subscription change, repair, or replacement candidate was attempted.

The official [Let's Encrypt rate-limit documentation](https://letsencrypt.org/docs/rate-limits/#new-certificates-per-exact-set-of-identifiers) describes the five-certificate limit and refill of roughly one certificate every 34 hours. Repeated clean installations are a documented cause. The supplied retry time permits retrying that failed request; it does not establish enough allowance for a new clean installation **and** another replacement. A new live attempt is not justified until that capacity is accounted for. Local/Ubuntu fixtures and CA staging can support further development, but staging is not production trusted-TLS acceptance.

Two operator command mistakes were preserved separately: an immediate curl preceded the asynchronous local client's listener startup, and a post-run assertion incorrectly expected a signed failed candidate to be deleted. The first was corrected after observing the actual owned listener, without repeating a product mutation; the second was corrected against the existing workflow, which retains signed failures as failed prereleases. Neither was hidden or used as passing evidence. Karing coordinate automation continued to return `noWindowsAvailable`; Karing results came from the Owner plus observed UI, not assumed actions.

## Validation and cleanup

Both Ubuntu native build/validation jobs, draft identity/asset checks, exact decision reproduction, and the signed manifest/attestation checks passed. Prior applicable local and Ubuntu fixture results for unchanged `b97b568` components were reused. This live attempt made no implementation changes.

Supported Complete removal returned `SOFTWARE-LIFECYCLE-COMPLETE-REMOVAL-COMPLETED`. Independent final VPS verification passed at `2026-09-13T13:26:43Z`: SBXR and sing-box files/package/account/services, owned certificate lineage/hooks/drop-in, qualification CA/hosts/firewall changes, staging, processes and test listeners were absent. SSH remained active; original Certbot 5893, snapd 27738 and core24 2124 revisions remained; all three original timers were enabled and active; the unrelated preflight directory remained.

Both old/replacement subscription URLs and proxy credentials failed from the outside after removal. The two exact owned Mac test clients were stopped and ports 32080/32081 were absent. Temporary client configurations, link files and credential-bearing captures were removed; useful redacted evidence was retained.

**Manual cleanup pending:** removal of the temporary Karing profile(s) has been requested but is not yet confirmed. The last observed original selection was `vm-argo-BWH.localdomain`; no automation changed it. Closing the test-link TextEdit windows and a follow-up state query timed out. Their files have been removed and their links are unusable, but window closure is unverified.

GitHub's failure path recorded the burned identity and retained `v3.1.72` as an immutable failed prerelease. Public latest remained `v3.1.0`; no stable release was published. No further candidate or qualification was started.

Key retained files: [CA rejection](../../../.scratch/acceptance/l72/04-ca-rejection.log), [redacted menu failure](../../../.scratch/acceptance/l72/04-renewal.redacted.log), [recorder](../../../.scratch/acceptance/l72/04-renewal-recorder.json), [submitted failure](../../../.scratch/acceptance/l72/failure.json), [final VPS verification](../../../.scratch/acceptance/l72/final-vps.json), [cleanup](../../../.scratch/acceptance/l72/cleanup.redacted.log), and [final release state](../../../.scratch/acceptance/l72/final-release-state.json).
