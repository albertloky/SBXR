# v3.1.73 MVP live acceptance — 2026-09-16

**Result: failed before signing and live execution; zero of five journeys run, no Acceptance Record.**

One candidate: tag `v3.1.73`, sequence `152`, assessed revision `7f04c12c75c29611f67feb780298a5e56d9dda68`, policy `mvp-live-v1`.

[Candidate workflow 35067639476](https://github.com/albertloky/SBXR/actions/runs/35067639476) · [Local run artifacts](../../../.scratch/acceptance/mvp-20260916-0706/)

## Original failure

At `2026-09-16T07:30:42Z`, the draft job's **Execute and verify the canonical draft decision chain** step failed while downloading `sbxr-linux-amd64.tar.gz` from GitHub's release-asset service: `read: connection reset by peer`. The step exited 1. This establishes a failed artifact download; the underlying cause of the connection reset was not investigated in this bounded acceptance task.

Both native candidate jobs had passed. The download failure prevented draft verification from completing, and signing and live acceptance were skipped. No environment approval, live product action, production certificate request, workflow rerun, replacement candidate or source repair followed. The original log remains private; [the redacted original failure](../../../.scratch/acceptance/mvp-20260916-0706/drafts-redacted.log) removes temporary download credentials.

The workflow finished with `failure`; `cleanup-unqualified` finished with `success`. Final GitHub queries confirmed the candidate release and tag were absent, and no burn tag exists because the attempt never reached signing. The failed v3.1.72 identity was not reused. See [final workflow state](../../../.scratch/acceptance/mvp-20260916-0706/run-final.json) and [final release state](../../../.scratch/acceptance/mvp-20260916-0706/final-release-state.json).

## Verification and readiness

Local and remote `main` matched the assessed revision. The tracked working tree was clean; all 10 pre-existing untracked acceptance, research and ADR documents were preserved.

[Native Verify 35065536788](https://github.com/albertloky/SBXR/actions/runs/35065536788) passed on Ubuntu amd64 and arm64 for the exact revision. Existing repair results in `native-repairs`, `rd` and `availability` were reused. Both native candidate jobs also passed their mandatory tests and package checks. Draft asset verification subsequently failed; no signed manifest or completed candidate trust result exists. No source repair or new feature was undertaken.

Fresh SSH checks established the disposable host was Ubuntu 24.04 amd64, free of SBXR and prior qualification state, with SSH and the original timers active. Original Certbot 5.8.0 revision 5893, snapd 2.76.3 revision 27738 and core24 revision 2124 remained. Direct outside connections bound to the Mac's `en0` reached TCP 80, 443 and 8443; all temporary readiness listeners closed afterward. The retained outside sing-box 1.13.19 package and executable matched their existing vendor receipt.

Karing 1.2.25.2802 matched the official package receipt and its installed signature verified. The Owner reported a fresh 226 ms latency result; the UI independently showed that value on the original selected connection. The Owner confirmed all earlier v3.1.72 temporary profiles and old link windows removed, with the original connection and DNS/routing/TUN/settings unchanged. UI automation did not establish profile deletion; that cleanup is explicitly Owner-confirmed. Latency is readiness evidence, not proxy-traffic proof.

The production CA budget is two requests: initial setup plus one reviewed replacement. The retained rejection's first retry time, September 14 at 16:16:25 UTC, accounts for one token only. The current official 34-hour refill interval accounts for the second by September 16 at 02:16:25 UTC. Fresh controlled-host Certbot logs show no later successful issuance, and the prior lineage is absent. This is an allowance calculation, not a CA reservation; no third request is budgeted. See [CA evidence and policy](../../../.scratch/acceptance/mvp-20260916-0706/ca-readiness.md) and [official policy](https://letsencrypt.org/docs/rate-limits/#new-certificates-per-exact-set-of-identifiers). No production certificate was requested for readiness.

## Journeys

| Journey | Result | Evidence and limit |
|---|---|---|
| `mvp-install`: setup, Check, outside proxy traffic, SSH | Not run | `acceptance-vps` skipped. Readiness traffic is not candidate proxy-traffic acceptance. |
| `mvp-subscription`: trusted HTTPS, wrong token, installed secrets, Karing import/manual refresh | Not run | No subscription enabled or candidate profile imported. Original-node latency and prior cleanup were readiness only. |
| `mvp-credentials`: UUID/URL rotation and revocation | Not run | No candidate credentials created or rotated. |
| `mvp-renewal`: reviewed certificate replacement | Not run | No certificate requested; the two-request allowance was unused by this attempt. |
| `mvp-removal`: restart and reviewed Complete removal | Not run | Candidate was never installed. Final host absence checks are cleanup/readiness evidence, not a passed restart/removal journey. |

All five results follow from the failed draft job and skipped live job in [run-final.json](../../../.scratch/acceptance/mvp-20260916-0706/run-final.json). No observation was submitted as a passed journey.

## Preparation observations

Initial direct HTTPS probes used synthetic addresses returned by Karing's resolver and timed out. A direct TLS DNS lookup followed by explicit destination resolution succeeded without changing Karing. Original errors remain in the run directory.

The initial local unsigned declaration contained a trailing newline and was refused by the existing canonical JSON validator before dispatch. Removing that byte passed the unchanged validator; the original document and refusal are retained. This did not start a candidate attempt or alter source. Exactly one candidate was subsequently dispatched.

## Cleanup and release boundary

Fresh final VPS verification at `2026-09-16T07:32:06Z` confirmed SBXR/sing-box state, owned lineage/hooks/drop-ins, qualification staging/transport/CA/hosts/firewall changes, and test listeners/processes absent. SSH remained active, the three original snap revisions and original timers remained, and the unrelated preflight directory was preserved. Product Complete removal was not necessary and was not counted as acceptance. See [final-vps.json](../../../.scratch/acceptance/mvp-20260916-0706/final-vps.json).

The temporary readiness listeners were closed. The task-owned SSH control connection was closed after the final check. No outside proxy clients or new Karing profiles were started; local test ports 32080/32081 remained absent. Earlier Karing temporary profiles/windows were Owner-confirmed removed, and no automation changed the original selection or persistent settings.

All 10 unrelated untracked documents still match the existing preservation record, and tracked Git files remain unchanged. Only this report and ignored run artifacts were created. No commit or push was made. See [workspace-preservation.json](../../../.scratch/acceptance/mvp-20260916-0706/workspace-preservation.json).

Public latest remains immutable stable `v3.1.0`. No stable release was published. The remaining release blocker is incomplete draft artifact verification followed by all five still-unexecuted live journeys. Automated verification passed; this fresh attempt did not establish live acceptance. Further qualification was not started.

Retained artifacts include the native build archives/receipts and declaration/preflight artifacts, complete original workflow logs in private files, redacted failure evidence, readiness and CA calculations, and final host/release/workspace observations under [the run directory](../../../.scratch/acceptance/mvp-20260916-0706/). Temporary download URLs and credential-bearing raw logs are excluded from this report.
