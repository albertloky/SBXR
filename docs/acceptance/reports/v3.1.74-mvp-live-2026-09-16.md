# v3.1.74 MVP live acceptance — 2026-09-16

**Not qualified. The credential journey timed out while its replacement Karing
confirmation was pending. Two journeys were accepted; no successful Acceptance
Record was produced. VPS and local process/file cleanup are verified; the Owner
has confirmed final UI cleanup.**

## Candidate and repair

- Candidate: `v3.1.74`, sequence `152`.
- Source: `261a5c8814bcf25d25a31cd14f597bfbe1735d2c`, committed and pushed to `main`.
- Policy: `mvp-live-v1`, the current five-journey clean-install procedure.
- [Native Verify 35086088427](https://github.com/albertloky/SBXR/actions/runs/35086088427): passed on Ubuntu amd64 and arm64 for this exact revision.
- [Candidate workflow 35092786994](https://github.com/albertloky/SBXR/actions/runs/35092786994): dispatched once; preflight, both native builds, draft construction, and signing passed; live acceptance failed.
- [Retained failed release](https://github.com/albertloky/SBXR/releases/tag/v3.1.74): immutable prerelease. The remote `release-burned/v3.1.74` record identifies this exact source, index, run, and sequence. This identity cannot be promoted or reused.
- Latest stable remains `v3.1.0`; no stable publication occurred.

The [asset-download repair](asset-download-repair-2026-09-16.md) was landed
without further source corrections. It preserves one fresh authenticated GET
for the reproduced connection reset, atomic destination replacement, and the
existing identity, size, digest, attestation, and qualification checks.
Independent Standards and Spec reviews found no issues. Retained applicable
transport tests passed on macOS and Ubuntu, along with Go ordinary/race coverage
and vet; the exact committed native Verify additionally passed. The new
candidate's authenticated download and signing paths completed successfully.
This does not claim that a live connection reset occurred during this attempt.

All three draft decisions and the signed manifest reproduced exactly. All four
downloaded assets matched their declared size, digest, and candidate identity;
native evidence digests and the GitHub attestation verified for the expected
workflow and source revision. Existing environment approvals were used under
the Owner's authorization for this single attempt.

## Live journeys

| Journey | Qualification result | Actual observations |
|---|---|---|
| `mvp-install` | **Accepted by collector** | Exact packaged install, reviewed setup, useful details, safe Check/Recover outcomes, outside proxy traffic returning the VPS origin, and preserved SSH. `01-observation.json`; advancement to `02-request.json`. |
| `mvp-subscription` | **Accepted by collector** | One successful initial production certificate issuance; outside trusted TLS 200 and one correct node; wrong-token 404; installed secret permissions and checked logs/arguments passed. Owner confirmed import, 166 ms node latency, manual refresh, and unchanged original connection/settings. `02-observation.json`; advancement to `03-request.json`. |
| `mvp-credentials` | **Failed: observation timeout** | UUID rotation ended the established old session after 24 successful requests. Old credentials failed without a timeout; replacement outside traffic worked. The unchanged link served the new UUID. Link rotation gave old-URL 404/new-URL 200 with identical proxy-node bytes. Owner later confirmed replacement refresh, 172 ms, and unchanged original settings, after the collector had stopped. No complete passing observation was submitted. |
| `mvp-renewal` | **Not run** | No reviewed certificate replacement was requested. |
| `mvp-removal` | **Not run as qualification** | Ordinary restart was not tested. Supported Complete removal subsequently passed as safety cleanup after the failed attempt; this does not qualify the full restart/removal journey. |

The credential request opened at `2026-09-16T13:26:15Z` and its observation
window ended at `2026-09-16T13:56:15Z` (21:56:15 Hong Kong). The collector retained
its original `actual_result: timeout` at `2026-09-16T14:01:18Z`, after its existing
five-minute submission allowance. Its original failure has `boundary: unknown`
and `host_state: Unknown`; those fields were preserved, not replaced with later
cleanup observations. Re-evaluating the retained failure with the committed
release tool reproduced its decision byte-for-byte: failed, stop test mutations,
and burn required.

The positive replacement Karing report was recorded at `2026-09-16T14:15:16Z` in
`03-karing-owner-late.json`. The exact earlier UI action time was not supplied;
no timestamp was backdated and the expired observation was not submitted.
The original failure facts and decision are retained under
`workflow-diagnostics/`, with the workflow failure state and remote burn record.

## Readiness and certificate use

Fresh preparation confirmed Ubuntu 24.04 amd64, original snap revisions/timers,
SSH, and a clean target. Outside probes used the Mac's physical `en0` interface;
TCP 80, 443, and 8443 readiness listeners were reachable and closed before the
attempt. The outside sing-box package matched its retained vendor receipt.
Karing 1.2.25 build 2802 matched current official metadata and its signature;
the Owner's original selected connection gave a fresh 167 ms readiness result.

Controlled-host CA history was refreshed before dispatch, and the retained
allowance calculation budgeted two production requests. This was a calculation,
not a reservation. This attempt requested only the initial certificate, which
succeeded. The one prepared replacement action was never executed. No issuance
retry, workflow rerun, or replacement candidate was launched.

## Cleanup

Supported Complete removal returned
`SOFTWARE-LIFECYCLE-COMPLETE-REMOVAL-COMPLETED`. Subsequent observations proved:

- Both former proxy clients were refused outside with zero response bytes and no timeout; both subscription URLs failed with connection refusal.
- Final VPS verification at `2026-09-16T14:18:11Z` found SBXR/sing-box files, package/account, services, owned certificate lineage/hooks/drop-ins, SBXR listeners/firewall rules, qualification transport/staging, and idle runtime lock absent.
- SSH remained active. Certbot revision 5893, snapd 27738, core24 2124, the original enabled/active timers, and the unrelated preflight directory remained.
- Both exact task-owned local client processes stopped and ports 32080/32081 closed. Their process identities were checked before termination.
- All 28 private run files were removed after retaining 14 redacted captures. The clipboard did not contain an exact owned credential value when checked.

**UI cleanup is complete by Owner confirmation**, recorded at `2026-09-16T14:34:47Z`:
the temporary Karing profile is removed, both generated TextEdit subscription-link
windows are closed without saving, and the original Karing selection/settings
remain unchanged. The earlier automatic close request returned
`AppleEvent timed out (-1712)`; the subsequent explicit confirmation is retained
in `owner-ui-cleanup.json`. This closes cleanup and does not change the failed
qualification result.

## Acceptance Record and handoff

There is **no successful Acceptance Record** and no qualified-candidate handoff
for stable publication. The repair is implemented and regression-tested; the
live results are limited to the observations above. Certificate replacement and
the full restart/removal journey remain unqualified for this candidate.

This attempt is stopped and its identity is unusable. A future attempt would
need separate authorization for a fresh candidate, refreshed readiness/CA
capacity, and a coordinated Karing session within the existing observation
windows. Do not resume this expired attempt, weaken its timing controls, or
carry its partial results into another candidate's Acceptance Record.

[Local run artifacts](../../../.scratch/acceptance/mvp-20260916-1038/) contain the
final `HANDOFF.md`, retained observations, original workflow failure,
reproduced failure decision, burn reference, and cleanup evidence. Credentials
and raw private captures are excluded from this report and have been removed
from the local run files. Unrelated acceptance, research, and ADR documents
remain untouched.
