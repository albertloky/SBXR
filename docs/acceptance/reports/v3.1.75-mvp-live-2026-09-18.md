# v3.1.75 diagnostic landing and MVP attempt — 2026-09-18

**The diagnostic fix landed and passed exact-commit verification. The single candidate is not qualified: four live journeys were accepted, but the fifth failed at ordinary service restart. Supported Complete removal also refused, so product cleanup is incomplete. No stable release was published.**

## Landed change and validation

[Commit cf2e89a](https://github.com/albertloky/SBXR/commit/cf2e89aae5cfa56ae316505d24f16d50b8a25069), `Retain candidate preflight failure diagnostics`, was committed and pushed on `main`. Only `.github/workflows/candidate.yml` and `release_candidate_workflow_test.go` were committed. Unrelated untracked documents were preserved. This report remains an uncommitted local report.

The confirmed diagnostic defect was lost context: completed `refused` or `unavailable` verifier outcomes could reach a silent failing assertion, and early failure did not retain the result. The implementation adds operation labels, distinguishes verifier execution failure from completed outcomes, reports the original status and allowlisted result state, preserves that status even if diagnostic writing fails, and retains available result JSON and diagnostic text on failed preflight.

The production GitHub adapter is unchanged. Removing only the diagnostic reporter, labels, failure-only upload and safe jq stderr redirection reconstructs the original workflow exactly. Acceptance assertions, artifact-trust checks, signing and release controls, authentication, retries, timeouts and qualification policy remain intact.

- **Regression-tested:** fresh validation passed all 17 real Bash/jq/subprocess/filesystem cases plus artifact wiring. Cases cover acceptance, refusal, unavailability, execution errors, child SIGTERM, missing/empty/invalid output, declaration and later git failure, diagnostic-write failure and verifier bypass. The retained pre-fix run demonstrates failures before the change. Valid unchanged integration, CLI outcome, vet, workflow and syntax results were reused from the diagnostic-fix run.
- **Landed verification:** [Native Verify 35313823760](https://github.com/albertloky/SBXR/actions/runs/35313823760) passed on the exact landed commit for Ubuntu amd64 and arm64. Both candidate builds also passed their native tests, race, vet, packaging and menu checks.
- **Hosted diagnostic behavior:** this candidate's preflight reported operation `write qualified preflight outputs`, status `0`, verifier `completed`, outcome `accepted`. The failure-only diagnostic upload was regression-tested but was not live-exercised by this successful preflight.

The original preflight failure in [workflow 35296858687](https://github.com/albertloky/SBXR/actions/runs/35296858687) remains unexplained. The diagnostic fix addresses the confirmed observability gap; it is not a proven repair of that run's upstream cause. The restart and cleanup failures below are separate observations from this candidate.

## Fresh preparation and exact attempt

Fresh production public-latest verification accepted stable `v3.1.0`, sequence `83`. A fresh full history of 147 releases, tags and burned identities established that `v3.1.75` / sequence `153` were unused before dispatch. The complete canonical declaration passed the existing validator. Local and hosted preflight passed; draft decisions and all four authenticated candidate assets were independently reproduced/verified. The signed manifest, native evidence digests and GitHub attestation verified before the live acceptance gate.

| Field | Exact value |
|---|---|
| Candidate | `v3.1.75`, sequence `153` |
| Source | `cf2e89aae5cfa56ae316505d24f16d50b8a25069` |
| Workflow | [35314735960](https://github.com/albertloky/SBXR/actions/runs/35314735960), run attempt `1` |
| Dispatch | Once, `2026-09-18T06:24:23Z`; no rerun or replacement |
| Declaration SHA256 | `3012da9a15c91ecc94336d392a07ad2844635953873875304a18f5821ba9d99a` |
| Release-index SHA256 | `3b4026ca92c6af03367c6b638df2e574e9a607dde5f09c613b6e6da1763b34b9` |
| Qualification-manifest SHA256 | `137318de28048d3833cb12fc89916a9e4ae03731d32b37b6fefaa9acf57aee03` |
| Signed attempt identity | `run-35314735960-attempt-1` |
| Scope | `mvp-live-v1`, `subscription-clean-install-repair`, no incoming sources |

Readiness used the actual disposable Ubuntu Server 24.04 amd64 host, outside TCP probes bound to the Mac's physical `en0` interface, unchanged original snap packages/timers, verified outside sing-box package, and installed Karing `1.2.25` build `2802` matching current official release metadata. Public TCP 80, product health, current certificate authority and CA history were rechecked before replacement.

Current CA policy and controlled-host history supported budgeting initial enablement plus one reviewed replacement. Capacity was inferred, not reserved, and could not exclude issuance elsewhere. Exactly those two public CA operations were used, with successful issuance logged at `06:51:24Z` and `07:38:39Z`. A scheduled check at `07:24:05Z` recorded “not due” and made no replacement; it was not used as the renewal journey's evidence.

## Actual live journeys

| Journey | Result and evidence |
|---|---|
| `mvp-install` | **Accepted.** Exact packaged candidate installed; reviewed setup completed; Running status, Check already-current and safe no-recovery result observed; real outside HTTPS proxy traffic exited through the VPS; ordinary SSH worked. Collector advanced at `06:48:58Z`. |
| `mvp-subscription` | **Accepted.** Reviewed initial enablement succeeded; normal public-trust outside TLS returned HTTP 200 and one correct plain VLESS node; wrong token returned HTTP 404. Exact production permissions and normal output/journal secret checks passed. Owner manually imported/refreshed the same Karing subscription, reported **170 ms**, and explicitly confirmed one node and preservation of selected connection, connection state and settings. Collector advanced at `07:07:07Z`. |
| `mvp-credentials` | **Accepted.** UUID rotated once. An established outside HTTPS session ended naturally during rotation after 26 successful requests; old credentials failed while their client remained running; replacement traffic worked. The unchanged link served the replacement identity. Link credential then rotated once: old URL returned trusted HTTP 404; new URL returned trusted HTTP 200; proxy artifact and traffic were unchanged. Owner reported **164 ms** in response to the requested same-profile replacement-link refresh. Collector advanced at `07:35:47Z`. |
| `mvp-renewal` | **Accepted.** One reviewed Replace subscription certificate action returned `PROXY-INSTALLATION-SUBSCRIPTION-CERTIFICATE-REPLACED`. Published, accepted and outside-loaded authority matched generation 2; normal public trust accepted a different certificate. The same link, unchanged proxy identity and fresh outside proxy traffic worked. Status was Available and renewal evidence healthy/idle. Collector advanced at `07:41:37Z`. |
| `mvp-removal` | **Failed at restart.** One ordinary restart returned shell status 0, but sing-box's startup `ExecCondition` exited 1 and systemd skipped starting it. Proxy state became Problem detected. Later Complete removal was safety cleanup only and refused; it does not turn this journey into a pass. No all-observed removal document was submitted. |

Only one numeric initial Karing latency value was reported; two separate 170 ms measurements are not claimed. The 164 ms reply was recorded in the context of the requested replacement refresh. An additional immediate preservation acknowledgment was not received and is not claimed; preservation was explicitly confirmed during the initial journey and at final manual profile cleanup. No agent Karing UI operation occurred, and no earlier candidate's observations were reused.

The initial outside certificate SHA256 was `84df2c0f95aa3b0a42c2a928b61c5020edcffc894ecd62e529159f75f8bd02ce`; the replacement was `1142a23808f6c408e4c4ac5f8fe04178c37e0ee426b6ec4ca5f974317cfb4e03`.

## Confirmed failure and limits of diagnosis

At `2026-09-18T07:47:47Z`, the normal command restarted `sing-box.service` and `sbxr-subscription.service`. The command returned 0. At `07:47:48Z`, sing-box was inactive/dead with `Result=exec-condition`, `MainPID=0`, and `ConditionResult=no`. Its installed drop-in invoked `/usr/local/bin/sbxr --proxy-start-authorize`; that process exited 1. The journal states the service was skipped due to its execution condition. Subscription serving restarted successfully and remained active.

Proxy configuration and subscription credential bytes were unchanged across the restart. SBXR details still accepted the protected configuration, package and Ownership Record, but reported that sing-box was inactive and the expected TCP 443 listener absent. These observations establish the startup authorization refusal, not its underlying cause. No second restart, authorization bypass, repair, timeout change, policy change or new candidate was attempted.

The supported Complete removal plan was reviewed and confirmed once for cleanup. Its original result was `PROXY-INSTALLATION-ACTION-REFUSED`, with **Failed safety check: Subscription absence** and the displayed correction “Inspect subscription material before retrying.” No removal commitment or completion was reported. The configuration and Ownership Record digests remained unchanged after this refusal. No finisher was offered; removal was not retried and protected resources were not force-deleted. The reason for this safety refusal remains unresolved.

The existing release validator accepted the failure facts with `stop_test_mutations: true`, `burn_required: true`, and safety cleanup `{host_state: "Problem detected", status: "incomplete"}`. The hosted diagnostic artifact exactly matches the locally validated failure facts and decision.

## Cleanup and release state

**Product cleanup remains incomplete on the VPS.** The installed SBXR executable, package/state/configuration, protected credentials, owned certificate lineage, subscription unit/firewall integration and renewal hooks remain. Sing-box is inactive and TCP 443 absent; the subscription service remains active on TCP 8443. The ordinary renewal timer remains enabled as originally configured. No additional certificate request was initiated by the agent.

Completed cleanup and verification:

- Both temporary outside sing-box clients were identified by their exact command/configuration, stopped, and their local ports proved closed.
- Owner confirmed removal of the temporary `SBXR v3.1.75 test` Karing profile and preservation of the original connection and settings.
- Fifty-two local private captures/configurations/link copies were removed after preserving sanitized evidence and original-byte digests. A scan found no known candidate credentials in remaining small run artifacts. The exact failure Ownership Record was retained after proving it contained no actual candidate credentials. Protected product credentials remain on the VPS because removal refused.
- The workflow removed its qualification gateway, temporary host routing/firewall rules and trust root. After downloading and verifying failure evidence, the stopped collector and the two task-owned helper files were removed with identity/content checks. Final inspection proved qualification paths absent.
- Final host inspection at `08:02:34Z` confirmed SSH active, original snap package identities and timers unchanged, and the unrelated preflight directory preserved. This is a partial cleanup result, not a clean host.

The candidate workflow completed **failure**; `cleanup-unqualified` completed **success**. The existing release process published [v3.1.75 as a failed prerelease](https://github.com/albertloky/SBXR/releases/tag/v3.1.75) and recorded `release-burned/v3.1.75` for sequence `153`, exact source/index and this workflow. The identity is now burned. No successful Acceptance Record exists.

Latest stable remains `v3.1.0`, sequence `83`; a fresh final production public-latest verification accepted it. `main` locally and remotely remains the landed diagnostic commit. No stable publication, rerun, replacement candidate or further repair was performed.

## Evidence and follow-up boundary

[Run artifacts](../../../.scratch/acceptance/mvp-20260918-0610/) contain `run.json`, final `HANDOFF.md`, declaration and trust evidence, regression/native/workflow logs, actual journey observations, certificate identities, original restart and cleanup-refusal results, sanitized transcripts/journal, exact failure Ownership Record, hosted failure artifacts, burn/release records and final cleanup checks.

Several run-local observer/document assumptions were corrected without product mutations: plain VLESS rather than base64; production `root:sing-box 0640` configuration permissions; a valid `null` renewal-attempt list after source-defined clearing; and canonical UTF-8 rather than ASCII-escaped nested failure facts. Original exceptions/refusals and correction notes were retained. These are separate from the confirmed service-start failure and removal refusal; no acceptance assertion or product control was weakened.

This task is stopped at the authorized failure boundary. A separate bounded investigation would be needed to explain the startup authorization refusal and the removal safety refusal, then plan supported cleanup of the remaining installation. No repair or further attempt is authorized by this report. The MVP scope also does not establish incoming upgrades, naturally due certificate renewal, automatic client refresh, Karing browsing, or the historical V4 matrix.
