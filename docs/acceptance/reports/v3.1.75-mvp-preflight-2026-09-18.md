# v3.1.75 MVP attempt — preflight stop, 2026-09-18

**Not qualified. The single authorized workflow failed during preflight, before
builds, signing, or any live journey. No certificate was requested. No release,
tag, burn record, or successful Acceptance Record was created.**

## Attempt and observed failure

- Proposed identity: `v3.1.75`, sequence `153`.
- Source: `261a5c8814bcf25d25a31cd14f597bfbe1735d2c`, unchanged local and remote `main`.
- Policy: `mvp-live-v1`, the current five-journey clean-install procedure.
- [Workflow 35296858687](https://github.com/albertloky/SBXR/actions/runs/35296858687): dispatched once at `2026-09-18T01:49:16Z`, run attempt `1`.
- Failed step: **Verify source state and unused release authority**, in `preflight`.
- The retained log ends at `2026-09-18T01:49:58Z` with exit code `1`, after Go toolchain/dependency downloads. It gives no command-specific failure diagnostic.
- Build, draft construction, signing, live acceptance, and candidate cleanup jobs were skipped. The workflow published no artifacts.

The exact failing command and root cause are **unresolved**. In particular, the
retained evidence does not establish a network failure, artifact-integrity
failure, product defect, or invalid declaration. The step contains several
silent shell assertions and redirects public-latest verification to a file that
was not retained in this run. A separate investigation must establish the cause;
this attempt was not rerun or followed by a repair cycle.

Local preparation had completed successfully before dispatch:

- Public latest verification accepted stable `v3.1.0`, sequence `83`.
- Fresh history covered 147 releases. Existing candidate preflight accepted the
  unused tag and sequence `153`, including burned-identity history.
- The complete MVP declaration passed the existing declaration validator.
- An initial local declaration file contained a trailing newline and was
  refused by the canonical JSON check. Removing only that newline produced the
  passing check before dispatch; the original refusal and correction note are
  retained. This was a local preparation-format correction, not a source change
  or an established cause of the later workflow failure.
- [Native Verify 35086088427](https://github.com/albertloky/SBXR/actions/runs/35086088427)
  remained successful on Ubuntu amd64 and arm64 for the exact unchanged source.
  Applicable existing validation was reused; no source repair or new regression
  test was made in this task.

## Readiness completed before dispatch

Fresh inspection established a clean Ubuntu Server 24.04 amd64 target, active
SSH, the required tools, and unchanged original Certbot/snap revisions and
timers. Outside probes bound to the Mac's physical `en0` interface reached TCP
80, 443, and 8443. Their temporary listeners closed after the probes.

The installed Karing version `1.2.25`, build `2802`, matched current official
release metadata. Its signature and retained package identity verified. The
Owner reported a fresh **173 ms** latency for the existing selected node and
confirmed availability for the upcoming manual steps. No candidate profile was
created, and the agent did not change Karing selection, connection state, DNS,
routing, TUN, or other settings.

The retained outside sing-box client package and executable matched their
existing vendor receipt. No outside proxy client was started.

Sanitized controlled-host CA history showed the latest successful issuance at
`2026-09-16T12:18:41Z`, with no subsequent issuance or rate-limit rejection and
no remaining renewal lineage. The known history and the current
[Let's Encrypt refill policy](https://letsencrypt.org/docs/rate-limits/#new-certificates-per-exact-set-of-identifiers)
supported budgeting initial enablement plus one reviewed replacement. That
calculation did not reserve capacity or exclude issuance elsewhere. Neither
budgeted request was used.

## Five-journey results

| Journey | Result |
|---|---|
| `mvp-install` | Not run; workflow stopped before live acceptance. |
| `mvp-subscription` | Not run; no subscription, certificate, or Karing import. |
| `mvp-credentials` | Not run; no credential rotation or replacement refresh. |
| `mvp-renewal` | Not run; no certificate replacement. |
| `mvp-removal` | Not run; no product installation existed to restart or remove. |

No previous candidate's partial observations were carried into this attempt.

## Cleanup and final state

Fresh final host inspection at `2026-09-18T01:51:46Z` confirmed product and
qualification resources absent, SSH active, and original snap packages, timers,
and unrelated preflight resources preserved. The temporary readiness listeners
had closed. Local ports 32080/32081 were absent; no client processes, private
candidate links, or temporary Karing profile had been created. Product removal
was unnecessary because installation never started.

Final GitHub checks found no `v3.1.75` release, tag, or
`release-burned/v3.1.75` record. The identity was not signed or burned; this does
not authorize reuse without fresh preflight and a new dispatch instruction.
Latest stable remains `v3.1.0`. No successful Acceptance Record or handoff for
stable publication exists.

Source, tests, dependencies, workflows, and existing documents were left
unchanged. Unrelated untracked documents were preserved. Disposable local build
output was removed; useful preparation observations and the original workflow
failure remain in the run directory.

## Handoff

This single-attempt task is stopped. The next bounded task is to establish the
preflight failure from reproducible evidence, including the public-latest
verification result and the exact failing assertion. Do not use another
candidate dispatch as the first diagnostic check. No additional workflow run,
certificate request, or stable publication was performed.

[Local run artifacts](../../../.scratch/acceptance/mvp-20260918-0142/) include
`HANDOFF.md`, `workflow-failed.log`, `workflow-run-final.json`,
`candidate-final.json`, the empty artifact listing, accepted local preflight and
declaration results, readiness observations, `cleanup-final.json`, and final
release/tag checks. The repository's current procedure and source remain the
authority; run-local copied scripts are only preparation artifacts.
