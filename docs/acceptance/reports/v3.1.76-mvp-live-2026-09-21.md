# v3.1.76 MVP attempt — 2026-09-21

**The single authorized attempt stopped on an operator-command error after
successful packaged installation, before proxy setup. No MVP journey was
accepted. No SBXR product defect was established. Public Complete removal
succeeded, final host cleanup was verified, and the candidate was burned.**

This report supersedes the readiness proposal only as a record of what happened
in this attempt. It does not add requirements to the current MVP procedure or
prove future host readiness.

## Candidate and preparation

| Field | Observed value |
|---|---|
| Candidate | `v3.1.76`, sequence `154` |
| Source | `323c5985bbc2d26951dd535b99a69d396741e31d`, `main` |
| Workflow | [35591785297](https://github.com/albertloky/SBXR/actions/runs/35591785297), attempt `1` |
| Dispatch | Once, `2026-09-21T11:01:00Z` |
| Policy | `mvp-live-v1`, clean installation, no incoming sources |
| Release-index SHA-256 | `5faaf1380c16fbd80aad1523ff505f23f72e9e0e6da7cdd24e3ec983e0b73282` |
| Manifest SHA-256 | `98174bb12fa23a49c9f0f10d91501280d669e1c977543dba14166dfd1ee1a957` |

The Owner approved the [bounded plan](v3.1.76-readiness-and-live-plan-2026-09-21.md).
Fresh observations confirmed the source, clean host, declared packages, log-parent
identity and child directories, idle locks, logging, and Karing availability.
All 148 published releases and their asset metadata matched the retained
preflight history. The public-latest verifier again accepted `v3.1.0`.

The temporary TCP listeners on 80, 443 and 8443 accepted connections from the
Mac bound to physical interface `en0` at 10:59:30–31 UTC. The listener process
exited and all three listeners were proved absent afterward. No firewall rule
or software was installed for this probe.

The fresh CA logs retained the prior issuance history. Their only new event was
the ordinary 09:51:16 UTC check reporting no renewals attempted. The conservative
capacity estimate remained above two certificates; **this attempt requested
zero certificates**.

Hosted preflight, both native architecture builds, draft construction and signing
passed. Before signing approval, the operator reproduced all three decision
stages and verified the four authenticated asset downloads, current draft state,
native evidence and fresh host facts. Before the separate live approval, the
signed manifest was reproduced, its GitHub attestation verified against the
exact source, and the declared package/journey bindings and host facts rechecked.
No approval relied only on the unsigned plan.

## Original failure and cause

The collector opened `mvp-install` at `11:22:43Z`. At `11:23:52Z`, the ordinary
packaged installer ran through the verified candidate transport. It returned:

```text
SOFTWARE-LIFECYCLE-INSTALL-INSTALLED
Installed v3.1.76
```

The next operator command was:

```sh
bash /run/sbxr-qualification/v3-packaged-live.sh remote-exact-candidate
```

It exited **127** at the retained `11:23:54Z` failure observation:

```text
bash: /run/sbxr-qualification/v3-packaged-live.sh: No such file or directory
```

This was an agent-authored operator mistake: a stale helper-location assumption
was reused without checking it against the current manifest-v3 transport.
The current workflow stages its transport under `/root/sbxr-qualification-v3`;
it does not install this helper under `/run/sbxr-qualification`. The separately
approved four-file operator staging also does not include that helper. This is
not evidence that the installer or an SBXR product check failed.

Shell fail-fast handling stopped before **Start setup**. Read-only inspection
showed Software Lifecycle Ready, Proxy status Not set up, Subscription status
Not enabled, and no Ownership Record. No protected-parent window was opened,
proxy configured, subscription enabled, Karing test profile created, or outside
proxy client started. None of the five journey observations was submitted as a
pass. The attempt was not resumed with an improvised helper or retried.

## Supported cleanup and failure handling

Before cleanup, a structured read independently matched the Installed Record
and actual executable to the signed candidate and downloaded amd64 archive.
An earlier local identity-output parser failed before invoking removal; the
structured read corrected that observer error, not a product failure. The
prematurely named `post-product-cleanup-host.json` was also collected before
removal and is not final cleanup evidence. The authoritative later receipts are
`safety-cleanup-host.json` and `final-host.json`.

At `11:27:12–14Z`, with the qualification-request environment cleared, the
packaged public **Complete removal** action ran once at the unchanged original
0775 log-parent mode. It returned
`SOFTWARE-LIFECYCLE-COMPLETE-REMOVAL-COMPLETED`. This was safety cleanup of the
installation-only state, not a passing MVP removal journey or a regression test
of restart/removal after certificate replacement.

The current validator accepted the original failure facts with
`stop_test_mutations: true`, `burn_required: true`, and completed safety cleanup
to Not installed. The facts were submitted atomically through the collector's
guarded result interface. The downloaded hosted failure facts and decision
matched the local validated bytes exactly.

The workflow ended **failure**, with `cleanup-unqualified` **success**. It
published [v3.1.76 as a failed prerelease](https://github.com/albertloky/SBXR/releases/tag/v3.1.76)
and created `release-burned/v3.1.76` for sequence 154 and the exact source/index.
There is no successful Acceptance Record. Latest stable remains `v3.1.0`.

## Final state and limits

The final host observation at **11:30:51 UTC** and supplemental checks proved:

- Product installation/state, owned resources and qualification transport paths
  absent, including broken-symlink checks; no task process or held product/Certbot
  lock remains. The original safe, unheld `/run/lock/sbxr.lock` remains unchanged.
- Workflow routing, firewall and trust additions removed. After hosted evidence
  was retained, the exact stopped collector files and the four reviewed operator
  files were removed with identity/metadata checks; their directories are absent.
- Original `/var/log` identity, root:syslog 0775 mode, child-directory metadata and
  rsyslog tmpfiles digest unchanged. SSH, logging and snapd are healthy;
  `rsyslogd -N1`, logrotate debug and the unique final syslog marker passed.
- Declared snap receipts and the ordinary Certbot timer remain unchanged; no new
  CA issuance appears. No product credentials were generated. No task outside
  client was started, and local client ports are free.
- Karing received no input or profile change; its existing connection was
  observed unchanged. No fresh candidate latency/import/refresh is claimed.

No product or tracked tooling source was changed, no commit made, no second
candidate dispatched, and no stable publication attempted. Pre-existing local
reports and research were preserved. Automated verification passed; the repaired
product's five live journeys remain **unproved** by this attempt.

The next bounded work should correct and locally rehearse the operator's exact
identity-check handoff against the current transport/staging contract, including
its absence/failure path, **before** proposing another live candidate. Do not
reuse this burned identity or use a new candidate as the first test of that
operator integration. No repair or further attempt was started here.

[Private run artifacts](../../../.scratch/acceptance/l21/) retain preparation,
approval, asset/attestation evidence, the original command/error, cleanup,
hosted failure records, burn record and final observations. The directory is
ignored and mode 0700. This report is an uncommitted local run record.
