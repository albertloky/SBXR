# v3.1.76 readiness decision and proposed live plan — 2026-09-21

## Decision and exact remaining gate

**GO for the two-certificate capacity budget and candidate preparation. The
unsigned candidate preflight and declaration are accepted. HOLD live execution
at the requested concrete plan-review gate.** No workflow has been dispatched,
no certificate requested, and no product journey started by this task.

The one outstanding technical readiness fact is **fresh outside TCP reachability
to the VPS on ports 80, 443 and 8443**, particularly port 80 for HTTP-01. The host
has no conflicting listeners or filtering rules, but that is not proof of the
provider/network path. Establish it with the bounded temporary-listener probe
below after this plan is approved. Do not repeat the historical certificate
audit or broad host investigation to resolve that narrow fact.

The concrete signing and live environment gates still apply. Candidate asset,
release-index and signed-manifest digests cannot be claimed before the build;
review the actual generated artifacts at those gates. This document proposes
one attempt, not a retry loop or stable publication.

## Capacity assessment: recent issuance, not thirteen detailed audits

The saved Censys search at `2026-09-21T08:08:59.319Z` captured all 13 unique
result cards and disabled pagination. Only these three result cards have
validity starting on or after September 14; their fingerprints match retained
local certificate receipts exactly:

| Actual local issuance (UTC) | SHA-256 fingerprint | Local receipt |
|---|---|---|
| September 16, 12:18:41 | `302d0c5b849bee2a74514bb512999bb0426b22092669467288c06e6ec4c519c4` | `mvp-20260916-1038/02-subscription.json` |
| September 18, 06:51:24 | `84df2c0f95aa3b0a42c2a928b61c5020edcffc894ecd62e529159f75f8bd02ce` | `mvp-20260918-0610/04-certificate-before.json` |
| September 18, 07:38:39 | `1142a23808f6c408e4c4ac5f8fe04178c37e0ee426b6ec4ca5f974317cfb4e03` | `mvp-20260918-0610/04-certificate-after.json` |

Those receipt paths are relative to `.scratch/acceptance/`. The latest Censys
raw certificate also matches the retained serial and exact validity period;
its SCTs are `2026-09-18T07:38:38Z`, one second before the successful local
issuance event. Search-card validity dates are not used as issuance timestamps.

Fresh host logs collected at `2026-09-21T09:02:05Z` retain every previously
filtered event, contain no new issuance/rate-limit event, and have no read
errors. No later result appears in the saved Censys search. The recent three
fingerprint matches, not merely the coincident total of 13, support this result.
Detailed identities and timestamps for the older ten certificates are not a
new acceptance requirement.

The [official Let's Encrypt rate-limit rules](https://letsencrypt.org/docs/rate-limits/)
were refreshed at `2026-09-21T09:02:47Z`: the exact-set bucket holds five and
refills one certificate per 34 hours; the broader IPv4 bucket refills once per
202 minutes. No ARI exemption is assumed.

At the fresh host observation time:

| Calculation | Estimated tokens |
|---|---:|
| Conservative: empty immediately after the latest issuance | **2.158546** |
| Reconstruction from the known retry anchor and three later issuances | **2.728268** |
| Broader IPv4 bucket, also empty after the latest issuance | **21.799175** |

The conservative two-token threshold was `2026-09-21T03:38:39Z`. Budget exactly
one initial enablement and one reviewed managed replacement. No certificate
request or Certbot invocation was used as a capacity probe.

This is an evidence-based operational assessment, not an authoritative CA
balance or reservation. CT can lag, and unobserved issuance elsewhere cannot
be ruled out absolutely by auditing older certificates. There is no evidence
of such issuance in the available public/local records. Recalculate if a new
issuance is discovered; refresh local activity before both budgeted actions.
Any CA refusal ends mutations rather than consuming a retry.

## Read-only host and client observations

The original inspector was streamed over authenticated SSH; a supplemental
read-only inspector completed at `2026-09-21T09:03:51Z`. Neither was staged on
the VPS. No permissions, packages, timers, services or firewall rules changed.

- The retained machine identity matches. OS is Ubuntu 24.04 amd64; the clock
  is synchronized. SSH, rsyslog and snapd are active; no failed systemd unit was
  reported. About 12 GB is available on the root filesystem.
- Inspected SBXR installation/state, owned units, package, account,
  certificate lineage, renewal configuration, hooks and qualification/staging
  paths are absent, including broken-symlink checks. Product ports are unused.
- No matching SBXR, sing-box, Certbot, menu-driver, permission-wrapper or
  supervisor process is present. No product/Certbot kernel lock is held.
  `/run/lock/sbxr.lock` is the existing root:root 0600 one-link, empty,
  non-symlink file, without xattrs. Its safe unheld presence is allowed;
  removal is not needed. The current maintenance runbook explicitly preserves
  that shared lock by design.
- `/var/log` remains real root:syslog 0775, device/inode `64770:34575`, link
  count 11, no xattrs. The nine immediate child-directory names match the
  earlier receipt; fresh per-child identities/metadata are retained.
- `/var/log/letsencrypt` is the existing real root:root 0700 directory, without
  xattrs. `/etc/letsencrypt` and `/var/lib/letsencrypt` are real root:root 0755
  directories. Certbot lock files are absent.
- Certbot **5.8.0 / revision 5893**, core24 **20260824 / revision 2124**, and
  snapd snap **2.76.3 / revision 27738** exactly match the retained byte receipts.
  The Ubuntu snapd package is `2.76+ubuntu24.04.1` amd64. No snap change is active.
  The observed next refresh is 13:08 UTC; do not overlap package maintenance
  with a protected-parent window.
- The ordinary Certbot timer remains enabled/waiting, last trigger September
  20 at 22:27:04 UTC, observed next trigger September 21 at **09:51 UTC**.
  Its service is inactive with last exit 0. There are no renewal configurations.
  Re-read the schedule before windows; never stop the timer or cross its next
  firing with a bounded menu process. A whole acceptance session need not be
  squeezed in before 09:51.
- `rsyslogd -N1` and `logrotate --debug /etc/logrotate.conf` both return 0.
  The rsyslog tmpfiles-rule SHA-256 remains
  `162fd61f2197334db713d1fea8bc36fc4fca78a4c13c4393451280742b7cc19f`.
  Debug validation did not rotate logs.
- Fresh firewall output has ACCEPT IPv4 filter policies and no filter rules;
  IPv6 output is empty. No owned firewall/hosts entry remains. Do not compare
  raw `iptables-save` hashes as a drift test: generated timestamps and counters
  change without rule changes. The fresh raw baseline is retained privately.
- Required host tools are available. Sing-box is correctly absent on the
  clean host; the retained official 1.13.19 amd64 `.deb` matches the planned
  SHA-256 `fb628b8cedf3e4c7cb32aa9c5103e0457e65ebb35ef510d041118836ef3b33bf`.
  Setup, not preflight, installs it at the ordinary 0775 log-parent mode.
- Karing is accessible to the agent through native Computer Use. The actual
  app is 1.2.25 build 2802; its strict signature verifies. Its retained DMG
  matches the current official GitHub release digest. Karing is connected to
  the pre-existing selected node with TUN enabled; no setting/selection or
  profile was changed. Its displayed old latency is **not** a fresh candidate
  observation. The agent, not a substituted Owner report, will do the required
  import and refresh during the live journeys.
- The outside sing-box 1.13.19 archive/executable match the retained official
  receipt. Mac `en0` is active; local ports 32080/32081 are free. Bind the test
  client's outbound to `en0`, rather than relying on Karing's selected route.

## Exact proposed candidate

| Item | Value |
|---|---|
| Repository / branch | `albertloky/SBXR`, `main` |
| Source commit | `323c5985bbc2d26951dd535b99a69d396741e31d` |
| Proposed tag / sequence | **`v3.1.76` / `154`** |
| Policy | `mvp-live-v1` |
| Support | `subscription-clean-install-repair`, no incoming sources |
| Journeys | install, subscription, credentials, renewal, removal, in that order |
| Limits | 1800 seconds per journey, 7200-second Karing boundary, 300-second validation/submission boundary |
| Package refresh | None; `after_snap_refresh` equals `packages` |

Local HEAD, remote main and GitHub main agree. The tracked tree/index are
unchanged; pre-existing untracked reports/research remain untouched.
[Verify 35579300774](https://github.com/albertloky/SBXR/actions/runs/35579300774)
passed native amd64 and arm64 checks at this exact commit. No additional
product/source repair was made in this task.

Fresh public-latest verification accepts stable `v3.1.0`, sequence 83. Full
published release collection covers 148 releases, with highest sequence 153.
Tag `v3.1.76` is unused. Current-source candidate preflight accepts the proposed
build and the complete unsigned MVP declaration also passes.

Retained preparation bindings:

```text
14abe4947f6376b31b917816c2258ebd54476de3ad9626b9834080d54bdc2060  declaration.json
681738b6dac1f6b807b903f0d0e1cd021f38d7db3ac52c0b8d2834990cec9adf  candidate-preflight-facts.json
```

These are preparation artifacts, not a signed release. Workflow-generated
attempt identity/start times will replace the declaration's placeholders at
signing, through the existing supported code. Recheck source/identity authority
if approval is delayed; do not silently move this plan to a different commit.

## Bounded execution sequence for review

### 1. Resolve the one network fact, then dispatch once

After approval, stream a small Python listener over the existing SSH connection:
bind only currently unused TCP 80, 443 and 8443, for at most 45 seconds. Make
the corresponding connections from the Mac using `IP_BOUND_IF` on `en0`.
Require all three to connect and require the listener process to exit and all
three ports to be closed afterward. Do not install software, stage host files,
open firewall rules, call Certbot or extend this probe on failure. Retain both
endpoint observations. If a port is unreachable, report that exact port and
stop before dispatch.

Once the probe and immediate source/host freshness gate pass, dispatch **once**:

```sh
cd /Users/Albert/Documents/Codex/SBXR
export TMPDIR="$PWD/.scratch/acceptance/g21/t"
mkdir "$TMPDIR" # Fresh temporary directory; refuse an existing path.
trap 'rmdir "$TMPDIR"' EXIT
bash .github/scripts/v3-candidate-dispatch.sh dispatch \
  "$PWD/.scratch/acceptance/g21/sbxr-release" \
  "$PWD/.scratch/acceptance/g21/candidate-preflight-facts.json" \
  "$PWD/.scratch/acceptance/g21/declaration.json"
```

At the signing gate, review the exact run/attempt, source, two native builds,
four assets, release-index, digests, draft state and reproduced decision chain.
At the separate live gate, verify the resulting manifest and its attestation,
package bindings and current host facts. Do not approve either on the strength
of this unsigned plan alone. Do not run `stable.yml` as part of this task.

### 2. Stage the reviewed operator files only after the live gate

Follow [the current temporary-parent plan](../mvp-protected-log-parent-2026-09-19.md)
without modification. Refuse an existing `/root/sbxr-mvp-log-parent`; create it
root:root 0700, stage only the four listed files, require regular one-link files
without xattrs/symlinks, and compare these exact source digests:

```text
0d5721dee62f86b6f8141b4935b8e941e4012af03a41a9d12c63f5d47e4c0895  mvp-protected-menu.sh
4358cb1ec189bd33518a081702355405e9110892cf2be7e8235671005e2959eb  with-protected-log-parent.sh
9861f9a16af051c9dcb7d21324ddb97a60690f3cf3fca7987f6c642d972a56cc  protected_command_supervisor.py
abf49f90e3380863c7f6f3d4bf3240f23b1e0c258ac77bc7afc03f34d025c66b  v3-menu-session.py
```

The two shell files use 0700; the two Python files use 0600. No recovery helper
or historical journey script is staged/replayed. No standing chmod window,
idle sentinel or direct Certbot command is authorized.

### 3. Execute and observe the five journeys

For every command below, require the current collector request to name the
appropriate journey and export its exact request path. Preserve private output
under the private run directory, not in this report.

```sh
export SBXR_QUALIFICATION_REQUEST=/root/sbxr-qualification-evidence/request.json
D=/root/sbxr-mvp-log-parent/v3-menu-session.py
E=/root/sbxr-mvp-log-parent/mvp-protected-menu.sh
```

Installation and the complete Start setup session are ordinary unwrapped
operations at 0775. After setup reaches Running, **each** numbered-menu process
below uses `--executable "$E"`, including status/details and disclosure.
The tables do not authorize sending all commands blindly: read each result and
stop on any unexpected outcome.

| Journey | Packaged operations and required outside observations |
|---|---|
| `mvp-install` | Install through the workflow's verified candidate transport and packaged `install.sh`; verify `remote-exact-candidate`. Run `python3 "$D" action 'Start setup' PROXY-INSTALLATION-SETUP-COMPLETE --confirmation yes --executable /usr/local/bin/sbxr`. After fresh pinned sing-box receipt verification, use wrapped View details, Check expecting `SOFTWARE-LIFECYCLE-CHECK-ALREADY-CURRENT`, and the safe no-recovery observation. Disclose the client configuration through the wrapped public action; use the separately verified outside client bound to `en0` for real HTTPS traffic through the proxy. Confirm SSH remains usable. |
| `mvp-subscription` | Fresh capacity/activity and port-80 readiness; run `python3 "$D" action 'Enable subscription' PROXY-INSTALLATION-SUBSCRIPTION-ENABLED --confirmation yes --executable "$E"` once. At restored 0775, require outside trusted TLS, one correct node, wrong-token rejection and safe file/output/log permissions. Through native Karing UI, import the actual link as a clearly named temporary test profile, obtain fresh node latency, manually refresh that same profile/link and preserve the user's selected connection, TUN, DNS, routing and settings. |
| `mvp-credentials` | Establish a real persistent outside session without reconnecting. Run wrapped `Rotate Client Identity` expecting `PROXY-INSTALLATION-CLIENT-IDENTITY-ROTATED` once. Require the established session to end, a new attempt with the old UUID to fail, and refreshed identity/traffic through the unchanged link to work. Then run wrapped `Rotate subscription link` expecting `PROXY-INSTALLATION-SUBSCRIPTION-LINK-ROTATED` once. Require old URL rejection, new URL success and unchanged proxy identity. In that same Karing test profile, update to the new link, refresh, and observe fresh replacement-node latency without changing the selected connection. |
| `mvp-renewal` | Establish Running / Available, published and loaded certificate agreement, idle healthy renewal evidence, working link/proxy and an outside trusted-TLS certificate fingerprint. Refresh the bounded CA/activity facts. Run `python3 "$D" action 'Replace subscription certificate' PROXY-INSTALLATION-SUBSCRIPTION-CERTIFICATE-REPLACED --confirmation yes --executable "$E"` exactly once. After restoration, require Available/healthy activation, a different certificate on a new normally validated outside TLS connection, unchanged link/proxy identity, and fresh proxy traffic. No forced direct Certbot call and no natural-timer claim. |
| `mvp-removal` | At original 0775, ordinary `systemctl restart sing-box.service sbxr-subscription.service`; wait for actual listeners and prove unchanged usable configuration, credentials, trusted HTTPS and outside proxy traffic rather than trusting exit 0. Run `python3 "$D" action 'Complete removal' SOFTWARE-LIFECYCLE-COMPLETE-REMOVAL-COMPLETED --confirmation remove --executable "$E"`. Prove owned resources/listeners absent, old outside access fails, SSH/logging/dependencies/unrelated resources preserved. Remove only the temporary Karing profile, stop only the task-owned outside clients, and clean their secrets. |

Immediately before each Running-phase invocation, freshly compare the declared
Certbot/snapd receipts and installed pinned sing-box receipt, idle processes and
locks, timer/refresh schedule, log-parent identity/children and absent
`window.state{,.control,.result}`. Use the normal driver maximum 900-second
timeout clipped by the collector deadline; if the next timer/refresh event is
within that bound, wait outside the window and recheck rather than stop the
timer. After each process, require original identity/0775, unchanged child
directory inventory and absent state/control/result files before outside/UI work.

### 4. Evidence, stop conditions and final cleanup

Record only actual observations within each current request's timing bounds.
Copy its required checks in exact order into the mode-0600 observation document
and atomically rename only a completed truthful document to `observation.json`.
No Karing result is prefilled and no old attempt's pass is reused. The current
collector validates/submits the record; it does not execute the journey.

Any unexpected product result, CA refusal, driver timeout, unsafe state,
wrapper restoration failure or incomplete journey stops further test mutations.
Retain the first failure and let existing failure/burn handling run; no automatic
second candidate or third CA operation. Supported cleanup cannot turn failure
into a passed journey. Apply the current permission plan's exact-quiescence
recovery only when its retained-state conditions hold; never force chmod,
delete unexplained state, replay the retired v3.1.75 recovery, or guess a PID.

After successful removal, prove normal transport teardown, original log-parent
and logging state, unchanged tmpfiles rule, no task clients/listeners/secrets,
and unchanged Karing selected connection/settings. Use the plan's unique final
syslog marker at this authorized post-live stage, not during read-only preflight.
Delete only the exact staged operator files and their empty directory once
quiescence and absent wrapper state are proved. Retain a secret-safe acceptance
record or the original failure record. Stable publication is a separate review.

## Evidence retained and limits

[Private local run directory](../../../.scratch/acceptance/g21/) contains the two
read-only inspectors and host receipts, supplemental firewall baseline, the
recent-issuance reconciliation and capacity computation, current CA rules,
current source/CI and release history, accepted preflight/declaration and exact
preparation scripts. It is ignored, mode 0700, and is not product source.

The current source/procedure remains authoritative. This work changed no
product source, made no commit, dispatched no workflow and made no VPS
configuration mutation. It establishes readiness observations and a reviewable
proposal, **not five passing live journeys**.
