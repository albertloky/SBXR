# v3.1.77 MVP attempt — 2026-09-22

**The single approved attempt stopped on an operator package-observation error
after successful installation, candidate identity verification and setup to
Running. No complete MVP journey was accepted. Public Complete removal succeeded,
final cleanup was verified, and the candidate was burned. No SBXR product defect
was established by the stopping condition.**

This report records this attempt only. It does not establish current readiness,
authorize another candidate or add requirements to the MVP procedure.

## Candidate and gates

| Field | Observed value |
|---|---|
| Candidate | `v3.1.77`, sequence `155` |
| Source | `477a0d7a32da683b0a0b00551696e363cfbc4e6c`, `main` |
| Workflow | [35677509069](https://github.com/albertloky/SBXR/actions/runs/35677509069), attempt 1 |
| Dispatch | Once, `2026-09-22T01:54:39Z` |
| Policy | `mvp-live-v1`, clean install, no incoming sources |
| Release-index SHA-256 | `139ea16d0e00c70cfa33de1409b7dde7641ef5b122b3370e7b8dd6247b550b9f` |
| Signed manifest SHA-256 | `c5bfa042e55cbe81503119d6d7163ffbfb64d1d44ec373457579c36728909048` |

The Owner approved the [bounded proposal](v3.1.77-mvp-preflight-2026-09-21.md).
Fresh checks confirmed exact source/CI, unchanged metadata for all 149 published
releases, accepted public latest, clean host, package receipts, original
log-parent identity/children, healthy logging, outside TCP 80/443/8443 and Karing
availability. The temporary network listeners exited and were proved absent.
The only new overnight Certbot event was the ordinary 22:27:03 UTC check with
no renewals attempted. The conservative two-operation capacity estimate remained
sufficient; this attempt ultimately requested **zero certificates**.

Hosted preflight, both native builds, draft construction and signing passed.
Before signing approval, the operator reproduced the three decision stages and
verified all four authenticated downloads against the current draft and native
receipts. Before live approval, the manifest was reproduced, its GitHub
attestation verified against the exact source, the package/journey declarations
compared and fresh host facts checked. The workflow's generated run/start/Karing
check timestamps were verified as its supported signing-time bindings, not
mistaken for unchanged unsigned placeholders.

## Original stopping condition

The collector opened `mvp-install` at **02:15:34 UTC**. The packaged installer
returned `SOFTWARE-LIFECYCLE-INSTALL-INSTALLED`. The corrected documented SSH
handoff streamed the unchanged reviewed module, invoked only `exact_candidate`,
and returned exit 0 before setup. The previous missing-helper-path defect did
not recur.

Start setup ran once, unwrapped at the original 0775 log-parent mode, from
02:17:01 to 02:17:26 UTC. It returned
`PROXY-INSTALLATION-SETUP-COMPLETE`, with Proxy status Running and locally proved
runtime. The subscription remained Not enabled. Its unwrapped menu displayed
the known protected-parent prerequisite, as anticipated by the operator plan;
that was not the stopping condition.

At **02:17:31 UTC**, the local post-setup assertion failed. The run-local
`inspect-window.py` only collected the installed package status when
`/var/lib/sbxr/sing-box_1.13.19_amd64.deb` still existed. It therefore returned
`installed_proxy_package: null`, and the local caller rejected it.

That assumption was wrong. At this source revision, the host adapter's
[`installPackage`](https://github.com/albertloky/SBXR/blob/477a0d7a32da683b0a0b00551696e363cfbc4e6c/internal/proxyinstallation/adapter/host/mutation.go#L889-L923)
verifies the temporary DEB, installs it, checks the service boundary, then removes
the artifact. Running-state package checks also deliberately require the held
installed status, `hi`, not ordinary `ii`. Review found the observer's latent
`ii` expectation was wrong as well; that branch was not reached in the original
failure because the temporary DEB was already absent.

Read-only follow-up established:

- Ownership phase Running, no subscription serving record, and configuration
  contents matching the Ownership Record.
- Installed sing-box status `1.13.19 amd64 hold ok installed`.
- Actual `/usr/bin/sing-box` SHA-256
  `031042edfd30a215e4c69d83eb7d13c194e6ef50c782e2e1308d9d8fa128454a`, exactly matching
  the binary extracted in memory from the approved, hash-verified DEB.
- Exact installed SBXR identity still bound to the signed candidate, with
  unchanged package receipts, original log-parent/children and idle locks.

This was an agent-authored observer error, not evidence that package installation
or the product's own Running check failed. Test mutations stopped. The observer
was not patched to resume the attempt; no permission window for test work,
certificate request, outside proxy client or Karing test profile was started.
No incomplete journey was submitted as passed.

## Supported safety cleanup and failure handling

The installed candidate and actual held package were independently verified
before cleanup. An initial local cleanup comparison also incorrectly compared
the Ownership Record's package-identity **string** with the declaration's
structured package object; it refused before invoking removal. The comparison
was corrected to the existing record representation and checked against the
actual binary receipt. This was cleanup assessment, not a resumed acceptance run.

At **02:21:28–02:21:48 UTC**, public Complete removal ran once through the
reviewed launcher, with `SBXR_QUALIFICATION_REQUEST` cleared. Its one bounded
safety-cleanup permission window returned
`SOFTWARE-LIFECYCLE-COMPLETE-REMOVAL-COMPLETED`. Afterward the original directory
identity/0775 mode, child inventory and absent wrapper state were verified.
Installed product/package/state and owned startup integration were absent.
This was safety cleanup, not a passing MVP removal journey or proof of removal
after certificate replacement.

The current validator accepted the original `mvp-install` failure facts with
`stop_test_mutations: true`, `burn_required: true`, and completed safety cleanup
to Not installed. Submission used the unchanged guarded collector interface.
Downloaded hosted failure facts and decision matched the local bytes exactly.

The workflow ended **failure**, with **cleanup-unqualified success**. It published
[v3.1.77 as a failed prerelease](https://github.com/albertloky/SBXR/releases/tag/v3.1.77)
and recorded `release-burned/v3.1.77` for sequence 155 and the exact source/index.
Latest stable remains `v3.1.0`. There is no successful Acceptance Record.

## Final state and next boundary

Final host receipts and exact cleanup at approximately **02:26 UTC** proved:

- Product, package, owned units/resources, qualification transport and task
  listeners/processes absent; the safe original unheld whole-host lock remains.
- Transport-owned routing, firewall and trust changes removed. After retaining
  hosted evidence, the stopped collector's exact two files and the four
  hash-verified operator files were removed, with their empty directories.
- Original `/var/log` identity, root:syslog 0775 mode, child metadata and rsyslog
  tmpfiles digest preserved. SSH, logging and snapd are healthy; rsyslog validation,
  logrotate debug and the unique post-cleanup syslog marker passed.
- Snap/Certbot receipts and timer units unchanged; no new CA log events or
  issuance. No client configuration was disclosed, no outside client started,
  and no Karing input/profile/selection/settings change was made.

No product or tracked tooling source changed, no commit was made, no second
candidate dispatched and no stable publication attempted. Existing local reports
and research were preserved. The first two product operations and corrected
identity handoff succeeded, but the five live journeys remain unproved here.

Before proposing another candidate, the appropriate next bounded work is a
**local rehearsal of the complete operator observation path across Not installed,
Running and removed states**, using the current package/ownership contracts.
Correct the observer and cover actual package-artifact removal, installed hold
status, receipt representation and failure/cleanup paths there. Another live
candidate must not be the first test of those assumptions. No such repair or
further attempt was started in this task.

[Private run artifacts](../../../.scratch/acceptance/l22/) retain the approved
preparation bindings, signing/live reviews, original failed observer, actual
product transcripts, package comparison, cleanup, matching hosted failure/burn
records and final host receipts. The directory is ignored and mode 0700. This
report is an uncommitted local attempt record.
