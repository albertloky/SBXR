# v3.1.77 MVP preflight — 2026-09-21

**GO for proposing one candidate attempt. Current-source candidate preflight
and the complete unsigned declaration are accepted. No candidate workflow was
dispatched, no release created or signed, and no product/CA journey started.**

This is a fresh preparation record, not live acceptance. The next approval is
for the bounded attempt below. Concrete artifact/signing and live reviews remain
mandatory after the build; an unsigned preflight cannot satisfy either gate.

## Exact proposed identity

| Item | Value |
|---|---|
| Repository / branch | `albertloky/SBXR`, `main` |
| Commit | `477a0d7a32da683b0a0b00551696e363cfbc4e6c` |
| Proposed tag / sequence | `v3.1.77` / `155` |
| Policy | `mvp-live-v1` |
| Support | `subscription-clean-install-repair`, no incoming sources |
| Journeys | install, subscription, credentials, renewal, removal |
| Limits | 1800 seconds per journey; 7200-second Karing boundary; 300-second validation/submission boundary |
| Package refresh | None; `after_snap_refresh` equals `packages` |

Local HEAD and remote main agree. [Verify 35610342946](https://github.com/albertloky/SBXR/actions/runs/35610342946)
passed both native architectures at this exact commit. Since the prior attempted
source, only the reviewed handoff documentation/tests/report changed; product,
candidate workflow, transport, identity helper and permission-wrapper bytes are
unchanged. The streamed identity boundary also has the separate real-SSH local
regression proof described in the [repair report](mvp-candidate-ssh-handoff-repair-2026-09-21.md).

Fresh public-latest verification accepts stable `v3.1.0`, sequence 83. The
current release-history collector inspected all **149 published releases**;
the highest sequence is 154. Remote tag/burn authority shows `v3.1.77` unused.
`v3.1.76` / 154 is burned following [35591785297](https://github.com/albertloky/SBXR/actions/runs/35591785297)
and must not be reused.

## Fresh readiness observations

- **VPS:** read-only observations at approximately 14:21 UTC match the prior
  final cleanup: no installed SBXR, owned units, product state, qualification
  directories, relevant processes or held product/Certbot locks. Ubuntu 24.04
  amd64 and the declared host identity match. The safe, unheld whole-host lock
  inode remains; it is not an installed product.
- **Protected parent and logging:** original real root:syslog 0775 `/var/log`,
  its metadata and child-directory identities match. The existing safe shared
  Certbot directories and rsyslog tmpfiles digest are unchanged. SSH, logging
  and snapd are active; rsyslog validation and logrotate debug both pass. No
  permission window was opened and no log rotation was performed.
- **Packages/activity:** installed Certbot 5.8.0 revision 5893, snapd 2.76.3
  revision 27738, core24 and the snapd Debian receipt match retained identities.
  No snap change is in progress. At observation, the next ordinary snap refresh
  was 19:33 UTC and Certbot timer 22:27 UTC. These times are observations, not
  reservations: recheck immediately before each proposed permission window.
- **Outside network:** a streamed, maximum-45-second TCP listener probe accepted
  fresh Mac connections bound to physical interface `en0` on **80, 443 and
  8443**, at 14:22:54–55 UTC. The probe exited; an independent listener query
  proved all three listeners absent afterward. No host file, software or
  firewall configuration was changed by this probe.
- **Mac/client:** Karing 1.2.25 build 2802 is available through native Computer
  Use, with its existing connection visible. Its strict code signature verifies;
  the retained DMG matches the current official release digest. No UI input,
  profile or setting was changed; displayed old latency is not a new candidate
  result. Retained pinned sing-box 1.13.19 package/client bytes match their
  receipts, and the outside archive matches fresh official GitHub asset metadata.
  `en0` is active and local client ports 32080/32081 are free.

## Two-operation certificate budget

Fresh filtered host logs retain the previous event set exactly: no new CA
activity or issuance since the prior attempt, which requested zero certificates.
The latest observed issuance remains September 18 at 07:38:39 UTC.

The [official rate-limit rules](https://letsencrypt.org/docs/rate-limits/) were
rechecked at 14:21 UTC. Using the conservative model of an empty exact-identifier
bucket immediately after that issuance, with one token replenished per 34 hours,
the estimate at the host observation is **2.314918 tokens**. The broader IPv4
limit is not the tighter constraint in this model. Budget only one initial
enablement and one managed replacement; assume no ARI exemption and no retries.

This is an evidence-based estimate, **not a CA balance query or reservation**.
The same-day public CT reconciliation from 08:08 UTC is retained, not presented
as newly fetched. CT can lag and neither it nor the VPS logs exclude unseen
issuance elsewhere. Refresh local issuance/activity and capacity before each
actual CA operation; new evidence or refusal stops the attempt, not another order.

## Bounded next attempt for approval

1. Refresh immediate source/identity and host facts. Do not silently move this
   proposal to another commit or identity. Dispatch the existing candidate
   workflow **once**, using the accepted preflight and declaration below.
2. At signing review, inspect that exact run/attempt, both native builds, draft
   state, four assets, release-index/digests and reproduced decision chain. At
   the separate live review, verify the resulting manifest and attestation,
   package/journey bindings and fresh host facts. These generated artifacts
   do not yet exist and have not been approved by this report.
3. Follow the [current MVP procedure](../mvp-live-acceptance.md) and the
   [current protected-parent plan](../mvp-protected-log-parent-2026-09-19.md).
   Stage only its four reviewed operator files, only after the live gate.
   Installation and Start setup use the ordinary unwrapped route; each later
   Running-phase menu process uses the existing launcher and restores 0775
   before outside/UI observations. Recheck package/activity, timer/deadline,
   directory and wrapper-state prerequisites before each window.
4. After installation and before Start setup, execute the
   [corrected streamed identity check](../mvp-live-acceptance.md#checking-the-installed-candidate-over-ssh)
   from this reviewed checkout. Its local source exists and passes `bash -n`.
   Require exit 0. Do not reuse the old `/run` helper invocation or the failed
   attempt's installation script.
5. Observe all five ordinary journeys with the actual packaged candidate and
   outside client: setup/traffic; HTTPS subscription and actual Karing import/
   refresh; UUID and link rotation; one managed certificate replacement; then
   ordinary restart, real readiness/traffic and public Complete removal.
   Preserve the user's Karing selection/settings. Submit only actually observed
   checks within each current collector request's timing bounds.
6. Any unexpected result stops further test mutations; retain the first failure
   and use existing failure/burn handling. No second candidate or third CA
   operation is authorized by this proposal. Supported safety cleanup does not
   turn failure into success. Prove final resource/listener cleanup and unrelated
   preservation. Stable publication remains a separate decision.

## Preparation artifacts and limits

The [private local run directory](../../../.scratch/acceptance/n21/) contains the
fresh observations, complete release collection, current-source release tool,
unsigned declaration, accepted decisions, source hashes and reviewed preparation
scripts. Binding hashes:

```text
bc464cb326871698ba66f3824f0114f6bbc6265f96c042dc151d4035c6d73d23  declaration.json
c2a65bfd1020f3adba6d79c2ca619d843176071932ffab87a134fc3ae2f47d3b  candidate-preflight-facts.json
```

The declaration retains the supported placeholder attempt identity; the existing
workflow binds the actual run and start time at signing. An initial locally
pretty-printed declaration was correctly refused by the canonical-JSON boundary.
With identical field values, the compact control passed. The local writer was
corrected, the declaration regenerated, and the supported `check` command passed.
No validator or product source was changed and no dispatch occurred during that
correction. Original refusal/control evidence is retained.

Temporary local directories were removed. No product/source edits, commit,
release mutation, CA request, persistent VPS change or Karing change was made.
This report is an uncommitted local preparation record. Existing reports and
research remain untouched. None of the five live journeys is established here.
