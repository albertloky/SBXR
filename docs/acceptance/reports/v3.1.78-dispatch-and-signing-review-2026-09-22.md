# v3.1.78 dispatch and signing review — 2026-09-22

**One candidate was dispatched. Preflight, both native builds, draft construction and the independent unsigned-artifact review passed. The workflow is waiting at signing; no signing or live approval was given.**

## Exact candidate and authorization

The Owner approved refreshing immediate prerequisites and one dispatch, followed by a stop at the concrete signing review. The corrected capacity assessment was used; a fresh crt.sh result or waiver was not required. This report is not an Acceptance Record or stable-publication approval.

| Item | Reviewed value |
|---|---|
| Workflow | [35716839090](https://github.com/albertloky/SBXR/actions/runs/35716839090), attempt 1 |
| Candidate | `v3.1.78`, sequence **156** |
| Source | `main`, `d69bf8fce444f5d35d458c2f7c68035fdff24eaa` |
| Draft release ID | `393649393` |
| Release-index SHA-256 | `a9572f1b17e722d0a305c78b4608a7b299f00250f6b5777b18d62962b0e6963d` |
| Policy / support | `mvp-live-v1` / `subscription-clean-install-repair`, no incoming sources |

## Immediate prerequisites

- Before dispatch, all 150 release records, tags, burn references, the latest 20 candidate runs and public Latest metadata matched the accepted preparation. The proposed identity was unused. Local/remote main, both native Verify jobs, operator-source hashes and the required-reviewer environment gate were checked. Prior supported-artifact verification was reused only after that fresh comparison; all historical downloads and regressions were not rerun.
- The Ubuntu host remained clean, with unchanged package receipts, original log-parent/child identities, safe locks and no active relevant writer. SSH/logging and read-only logging validation passed.
- Bounded TCP probes reached ports 80, 443 and 8443 through physical `en0`. The remote probe exited and an independent check proved the temporary listeners absent.
- Karing 1.2.25 build 2802 was observed through native Computer Use without input/settings changes. Strict/deep code-signature verification, four retained package/client hashes, fresh official asset metadata, physical-interface availability and free local client ports passed. Old displayed latency is not candidate evidence.
- The only new filtered CA event since the corrected review was `no-renewals-attempted` at 09:51:07 UTC. There was no new observed order, issuance or rate-limit event. The conservative exact-set estimate at the pre-dispatch host observation was **2.908742**, supporting the planned two-operation budget under the recorded assumptions.
- Post-build host observation at **2026-09-22T10:52:52Z** again confirmed the clean host, unchanged package/log identities, idle writers/locks and no new CA events during the build. The conservative estimate was **2.918734**. Observed next snap refresh was 15:15 UTC and certificate timer 22:27 UTC; neither is a reserved window.

The estimates assume no unseen issuance and are not an authoritative CA balance or reservation. The same-day official-rule receipt and independently reconciled historical public evidence were reused; no new public-history search was needed. Refresh activity and capacity before actual CA operations. No third request or automatic retry is authorized.

## Generated-artifact review

- Hosted preflight, both native architecture jobs and draft construction passed. Those build jobs include ordinary and race tests, vet, module verification, package checks and native startup/menu checks. This does not claim new execution of the separately evidenced isolated root-only SSH/window regressions or live acceptance.
- The hosted declaration exactly matches the submitted unsigned declaration. Its validation decision and all three release decisions were independently reproduced byte-for-byte with the retained source-bound release tool: preflight accepted, draft construction actions-required, draft verification accepted.
- Four assets were downloaded through the source-owned authenticated reader. Sizes/digests match the current draft, hosted download receipts and both native build receipts. The index has the exact source, tag, sequence and support scope. Local supported-package verification passed for amd64 and arm64.
- A final draft read confirms the same asset receipts and unpublished state. The exact run has one pending `acceptance-vps` signing gate and an empty approval history. No signed manifest/attestation exists yet and the live job has not started.

| Asset | Bytes | SHA-256 |
|---|---:|---|
| `install.sh` | 44917 | `e9c788b795a88da123840333d32b78e6fb355c955e4b5904beacb90359a8b489` |
| `release-index.json` | 612 | `a9572f1b17e722d0a305c78b4608a7b299f00250f6b5777b18d62962b0e6963d` |
| `sbxr-linux-amd64.tar.gz` | 14901632 | `6453fe64e6319f9301e5887759c5245d9ebb7ec73b011ad9d2094bcdcc4a5619` |
| `sbxr-linux-arm64.tar.gz` | 13748929 | `7b63f7a45b959d1a52b9498425253714256654563bc5308170bce4f1addbb78d` |

## Stop and next decision

**Stop at signing, as authorized.** The unsigned candidate is ready for the Owner’s signing decision on this exact run. Refresh immediate readiness if approval is delayed. Signing creates the bound qualification manifest and attestation; it does not authorize installation or certificate requests. Review those signed artifacts and fresh readiness before the separate live approval.

Later live work, if approved, must use the current MVP procedure, protected-parent plan, source-owned streamed observer and `exact_candidate` handoff—not historical scratch journey scripts. No second candidate, automatic retry, third certificate operation or stable publication is covered.

## Evidence, local check corrections and preservation

Private evidence is in [`.scratch/acceptance/n22-dispatch/`](../../../.scratch/acceptance/n22-dispatch/), especially `dispatch-result.json`, `release-readiness.json`, `capacity-assessment.json`, `signing-review.json`, `presign-host-review.json`, `run-final.json`, `draft-final.json`, `pending-final.json` and `approvals-final.json`. Raw host/client observations remain private.

Two local bookkeeping/comparison details are retained rather than suppressed:

- After dispatch returned its GitHub URL, zsh rejected assigning its reserved `status` variable while recording the exit. The dispatch was not repeated. An independent run-list comparison confirmed exactly one new matching run.
- An additional raw firewall-hash comparison was unsuitable: `iptables-save` includes timestamp comments and changing chain counters. Two retained read-only outputs have identical policies/rules after removing only those dynamic fields, with no SBXR-owned rules. The earlier and later host receipts contain hashes rather than raw rules, so no complete across-build rule comparison is claimed. The final Latest metadata comparison separately found only one nondecreasing asset download counter (445 to 447); identity and asset receipts were unchanged. These were local observation issues, not candidate failures or new product requirements.

All 23 pre-existing untracked documents remain byte-identical. This report is new and uncommitted. Tracked source is unchanged; no commit or push occurred. The private local temporary directory was removed. No operator files were staged, no permission window was opened, no product was installed, no CA request or live journey occurred, and no recurring automation or subagent was created. The existing browser task was untouched.
