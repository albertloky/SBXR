# v3.1.78 MVP attempt — 2026-09-22

**Both Owner approvals were applied. Signing and independent signed-artifact verification passed. The live attempt stopped before product installation because the source-owned window observer rejected a snap image with two hard links. No MVP journey was accepted. Cleanup passed and the candidate was recorded as failed and burned; stable publication did not occur.**

This report records one attempt. It does not authorize a retry or alter the current MVP procedure.

## Candidate and approvals

| Field | Observed value |
|---|---|
| Candidate | `v3.1.78`, sequence `156` |
| Source | `d69bf8fce444f5d35d458c2f7c68035fdff24eaa`, `main` |
| Workflow | [35716839090](https://github.com/albertloky/SBXR/actions/runs/35716839090), attempt 1 |
| Release ID | `393649393` |
| Release-index SHA-256 | `a9572f1b17e722d0a305c78b4608a7b299f00250f6b5777b18d62962b0e6963d` |
| Signed manifest SHA-256 | `26f6ff823551497e3770f500b7c2052bbbdb37e3b7514fe19b31ee6b054a2aab` |
| Scope | `mvp-live-v1`, clean install, five normal journeys, no incoming sources |

The Owner said, “Approve this signing and approve this run.” This authorized the two concrete gates for the existing candidate, conditional on their checks—not another candidate, automatic retry, a third certificate operation, or stable publication.

The [unsigned review](v3.1.78-dispatch-and-signing-review-2026-09-22.md) was refreshed against the current run, draft asset receipts, local/remote source, native CI, supported operator-source identities and public Latest. All four retained authenticated assets were rehashed. Fresh host observations established unchanged packages, original log identities, clean product footprint, idle writers/locks and healthy SSH/logging. Fresh physical-interface TCP probes reached ports 80, 443 and 8443; the temporary listeners exited and were independently proved absent. Karing was observed without input or settings changes; its package/signature and outside-client receipts were checked.

There were no new filtered CA events. At the 14:18:25 UTC host observation, the conservative exact-set estimate was 3.019493 under the previously reviewed no-unseen-issuance assumption. It was not a balance query or reservation. The same-day rule receipt and reconciled historical public evidence were reused; no new public-history lookup was required or performed.

After signing succeeded, the qualification manifest was independently reproduced byte-for-byte, its native evidence and declaration bindings checked, and its GitHub attestation verified against the exact source/workflow. Signing-time generated fields were checked as generated fields, not compared to unsigned placeholders. Fresh host readiness was checked again before the separate live approval. GitHub records both approvals.

## Original stopping condition

The four unchanged reviewed operator files were staged under the protected operator directory, with their modes, ownership and digests verified. No permission window was opened.

At **2026-09-22T14:24:49Z**, the first streamed source-owned `mvp-inspect-window.py` invocation, in `not-installed` phase, returned exit 1:

```text
MVP_WINDOW_REFUSED unsafe-file:/var/lib/snapd/snaps/certbot_5893.snap
```

The collector subsequently opened `mvp-install` at **14:25:22 UTC**. No packaged installer, menu action, proxy client, subscription request or certificate operation ran. The earlier refusal was preserved with its original timestamp; it was not rerun after the collector started or bypassed to begin installation.

Read-only diagnosis established:

- The observer applies its generic `protected_file` requirement of exactly one hard link to each snap image.
- Certbot, core24 and snapd images each have **two hard links**, with one name under `/var/lib/snapd/snaps` and one under `/var/lib/snapd/cache`.
- Both names resolve to the same respective device/inode. The files are regular root:root 0600 files, not symlinks, without extended attributes. Their inspected ancestor directories are root-owned and not group/other writable.
- Versions, sizes and SHA-256 values remain identical to the reviewed receipts. Product paths remain absent and the original log-parent identity/mode remains unchanged.

This locates the blocker in the operator's single-link assumption for snap images. It does not establish an SBXR product defect: the packaged product was never installed or executed. No permissions, cache links, package files, snap settings or product trust checks were changed to make the observer pass.

The next targeted work is to review the snap-image metadata contract in the observer and cover the real hard-linked layout in its regression fixture, while retaining the applicable strict checks for operator/state/private files. That repair and another candidate are not part of this completed attempt.

## Failure handling and cleanup

The original refusal was submitted through the supported failure interface. The real release validator returned `failed`, `stop_test_mutations: true` and `burn_required: true`. Downloaded hosted failure facts and their decision match the locally submitted bytes exactly. No incomplete journey was submitted as an all-observed pass.

The workflow's live job failed and its failure-finalization job succeeded. The exact candidate was published only as a failed prerelease, with the burn record independently checked and its attestation verified. Public Latest remains `v3.1.0`, with unchanged identity and asset receipts. No stable publication was approved or performed.

After the workflow removed its temporary delivery transport, the operator checked and removed only the exact four staged files and the stopped collector's matching request/result files. Final independent observations prove:

- No SBXR installation, owned service/resource footprint, qualification transport, operator directory, collector directory, test writer or held product/Certbot lock remains. The original unheld host lock file is preserved.
- The original log-parent/child identities and 0775 mode, package receipts, Certbot directories, renewal units/timer and rsyslog rule digest are unchanged.
- SSH, logging, snapd and the ordinary renewal timer are healthy. `rsyslogd -N1`, logrotate debug and a fresh unique syslog marker passed.
- No CA event was added. **Zero certificate requests and zero permission windows** occurred.
- No outside proxy client or Karing test profile was started; Karing input/settings were untouched. Local client ports remain free.

Owned firewall-rule absence was checked with successful firewall reads. Raw full-firewall hashes were not treated as stable identities because comments/counters change; this is not a claim of a byte-identical full firewall dump.

## Evidence and local bookkeeping

Private evidence is in [`.scratch/acceptance/l22-78/`](../../../.scratch/acceptance/l22-78/), particularly `authorization.json`, `presign-review.json`, `signed-review.json`, `live-readiness.json`, `before-install.receipt.json`, `before-install.stderr`, `original-refusal-inspection.private`, `snap-link-inspection.private`, `failure-facts.json`, `failure-decision.json`, `hosted-diagnostics/`, `cleanup.private`, `final-host.json`, `final-prerequisites.json`, `final-workflow.json`, `final-burn-record.json` and `completion.json`.

The first local failure-document encoding used Python ASCII escapes for Unicode in the embedded signed boundary. Canonical validation refused it before any submission. The original local draft was retained; encoding was corrected to preserve the original UTF-8 boundary bytes, and validation then passed. This was a local evidence-serialization correction, not a resumed live test or a changed failure outcome.

Tracked source is unchanged. All 24 pre-existing untracked documents remain byte-identical. This report is new and uncommitted; no commit, push, retry, subagent or recurring automation occurred. The disposable local temporary directory was removed.
