# v3.1.78 MVP preflight — 2026-09-22

> **Assessment correction, 07:16 UTC:** the later investigation recovered and
> independently checked the September 21 reconciliation of the three relevant
> recent certificates. Fresh host logs contain no subsequent issuance. The
> evidence supports the two-operation budget; a successful fresh crt.sh lookup
> is not a mandatory MVP gate. See the [corrected assessment](v3.1.78-capacity-followup-2026-09-22.md#corrected-assessment-after-investigation).
> The original observations and earlier hold below are retained as history.
> No dispatch, signing, live acceptance or stable publication is established.

**Current-source candidate preflight and the unsigned declaration are accepted.
Host, package, outside-network and client preparation checks passed. The fresh
public certificate-history lookup failed, so the certificate-capacity review
remains conditional, not cleared. No candidate was dispatched.**

This is a preparation handoff, not an Acceptance Record or an unqualified GO.
It authorizes no build, signing, host mutation or certificate request. Resolve
the outstanding capacity assessment and refresh immediate prerequisites before
requesting approval for one candidate attempt. The later concrete signing and
live reviews remain mandatory; their generated artifacts do not yet exist.

## Proposed identity and source

| Item | Value |
|---|---|
| Repository / branch | `albertloky/SBXR`, `main` |
| Source commit | `d69bf8fce444f5d35d458c2f7c68035fdff24eaa` |
| Proposed tag / sequence | `v3.1.78` / `156` |
| Policy | `mvp-live-v1` |
| Support | `subscription-clean-install-repair`, no incoming sources |
| Journeys | `mvp-install`, `mvp-subscription`, `mvp-credentials`, `mvp-renewal`, `mvp-removal` |
| Limits | 1800 seconds per journey; 7200-second Karing boundary; 300-second validation/submission boundary |
| Package refresh | None; `after_snap_refresh` equals `packages` |

Local HEAD and remote main match. [Verify 35688634270](https://github.com/albertloky/SBXR/actions/runs/35688634270)
passed on both native amd64 and arm64 at this exact source. Relative to the
previous attempt, the landed change repairs operator observations and SSH test
fixture account isolation, with regressions and documentation. Product,
candidate workflow, transport, installed-candidate identity helper, menu driver
and the four staged operator files are unchanged.

The [observer repair report](mvp-operator-observer-repair-2026-09-22.md),
[deadline investigation](mvp-driver-deadline-investigation-2026-09-22.md), and
[SSH fixture repair report](mvp-ssh-fixture-readiness-repair-2026-09-22.md) retain
the separate local boundary proofs and their limits. Native Verify is not
misrepresented as execution of root-only SSH/namespace tests; those have their
own isolated Linux receipts. No new live candidate was used to test these fixes.

Fresh supported public-latest verification accepts stable `v3.1.0`, sequence
83. The source-owned history collector read all **150 published releases**,
with highest sequence **155**. There were no drafts. Remote tag and burn
authority show `v3.1.78` unused; `v3.1.77` / 155 is burned after the
[previous attempt](v3.1.77-mvp-live-2026-09-22.md) and must not be reused.

At the final metadata recheck, release identity/asset metadata, main, tags,
burns and the 20 most recent candidate-run records were unchanged. A full JSON
equality check initially refused differences in download counters. Inspection
established that every difference was a nondecreasing asset `download_count`,
consistent with this preparation's authenticated downloads; no release identity
or asset receipt changed. The exact deltas are retained, not suppressed as
network absence or a release change.

## Fresh host and client observations

- **Host, 06:22:36 UTC:** the same Ubuntu 24.04 amd64 identity and clean
  post-removal footprint. SBXR, sing-box, owned units/state, qualification
  directories and relevant processes are absent. Product/Certbot locks are
  unheld; the original safe whole-host lock inode remains.
- **Protected parent:** original real root:syslog 0775 `/var/log`, directory
  identity, child identities, existing safe Certbot directories and rsyslog
  tmpfiles digest match the prior final cleanup. SSH, rsyslog and snapd are
  healthy. `rsyslogd -N1` and logrotate debug pass; no real rotation or permission
  window was performed.
- **Packages/activity:** Certbot 5.8.0 revision 5893, core24 20260824 revision
  2124, snapd 2.76.3 revision 27738 and their receipts match. No snap change or
  renewal process is active. The observed next snap refresh was 08:55 UTC and
  Certbot timer 09:51 UTC. These are observations, not reserved idle windows;
  packages, writers, timer margins and collector bounds need fresh checks.
- **Outside network, 06:26:12 UTC:** a streamed, maximum-45-second listener
  probe accepted Mac connections bound to physical `en0` on TCP **80, 443 and
  8443**. It exited and an independent query proved the listeners absent. No
  host file, firewall rule, software or persistent configuration was changed.
  TCP reachability is not candidate proxy traffic or trusted HTTPS acceptance.
- **Client, approximately 06:24 UTC:** Karing 1.2.25 build 2802 is available
  through native Computer Use on macOS 26.6.2. Strict/deep code-signature
  verification passes; the retained DMG matches fresh official latest-release
  metadata. Pinned sing-box 1.13.19 DEB and outside-client bytes were rehashed,
  and the outside archive matches its official asset metadata. `en0` is active
  and local ports 32080/32081 are free. No Karing input, profile, selection or
  settings changed; old displayed latency is not new candidate evidence.

## Certificate capacity: conditional, with a failed public refresh

Fresh filtered host logs exactly match the final-cleanup event set: no new CA
activity or issuance. The latest observed issuance remains
**2026-09-18T07:38:39Z**; the previous candidate requested zero certificates.

The [official rate-limit rules](https://letsencrypt.org/docs/rate-limits/) were
saved at **06:29:00 UTC**, with source update date August 5, 2026. Assuming an
empty exact-identifier bucket immediately after that latest issuance and no
unobserved subsequent issuance, the 34-hour refill model yields **2.786250
tokens** at the host observation. The broader IPv4 model yields 28.138366 and
is not the tighter constraint. Plan only initial enablement and one managed
replacement: **two operations, no automatic retry and no ARI exemption assumed**.

This arithmetic is not a CA balance query or reservation. After the original
browser task disappeared, the Owner explicitly approved reopening these
read-only checks. In the replacement task, [crt.sh](https://crt.sh/) returned
**404 Not Found** for the simple identifier search at 06:33:08 UTC. Its explicit
exact `iPAddress` SAN search timed out loading; inspection at 06:35:45 UTC
retained **502 Bad Gateway**. Neither is a certificate-history result. Bounded
lookups stopped; no issuance was used as a capacity probe.

The September 21 Censys result remains historical and partial: 13 result cards
matched the retained issuance count and the latest certificate was fully
matched, but remaining record details were not reconciled before its guest
credit limit. No new Censys request, account creation, credit-limit workaround
or purchase occurred. CT can lag, and neither local logs nor CT excludes
issuance elsewhere. Do not turn this failed refresh or the older partial result
into a claim of complete current history. The capacity review remains open;
this report does not silently waive it or change the MVP policy.

## Bounded next attempt, only after readiness and approval

1. Settle the outstanding capacity review, refresh source/tag/burn and immediate
   host/client prerequisites, and retain this exact source and identity. With
   explicit subsequent approval, dispatch the existing candidate workflow once.
2. Review the exact generated run/attempt, both native builds, draft, four
   assets, release index and reproduced decisions before signing. Separately
   verify the signed manifest, attestation, package/journey bindings and fresh
   host readiness before the live gate. The unsigned declaration's supported
   attempt/start placeholders are bound by the existing workflow at signing.
3. Follow the [current MVP procedure](../mvp-live-acceptance.md) and
   [protected-parent plan](../mvp-protected-log-parent-2026-09-19.md), not old scratch
   journey scripts. Stage only the four reviewed operator files after the live
   gate. Stream the new source-owned observer; it is not a fifth staged file.
   Construct its expectations from current reviewed receipts, including the
   binary extracted from the verified DEB, not the DEB digest itself.
4. Use the documented observer phases at their actual boundaries. After
   installation, stream `exact_candidate` from the reviewed source and require
   exit 0 before Start setup. Installation/setup use the unwrapped route at
   0775. After Running, each complete menu process uses the launcher; restore
   original metadata before outside and Karing checks. Compare package,
   activity, timer/deadline, directories and wrapper state around every window.
5. Observe only the five ordinary journeys with the real packaged candidate,
   outside traffic, trusted TLS and actual Karing import/refresh. Preserve
   Karing's selection/settings. Refresh capacity before each of the two CA
   operations. Submit only observations actually made within the current
   collector request's bounds.
6. Stop on the first unexpected result and retain it; use the existing
   failure/burn and legally reviewed safety-cleanup paths. No second candidate,
   third certificate order or automatic retry follows from this proposal.
   Prove final cleanup and unrelated preservation. Stable publication remains
   a separate decision.

## Retained artifacts and scope

The private, ignored [run directory](../../../.scratch/acceptance/n22/) contains the
release collection, observations, failed public responses, capacity assessment,
source hashes, reviewed preparation scripts and accepted current-source
decisions. Bindings:

```text
c5d07dc5efaba31f83eafbde7dc0a959904c27c25dea91df3e21d8c0457c7303  declaration.json
4162e3e7574ecd3c867b8ae4a10385d2c3d8dd8c10ce337856a46cdbb1253104  candidate-preflight-facts.json
```

Only the dispatch helper's **check** branch was called. No candidate build,
draft creation, signing, dispatch, CA request, product mutation, permission
change, source edit, commit or push occurred in this preparation. Temporary
local directories and TCP listeners are gone; useful private evidence remains.
The browser task is left stopped on the unresolved service error, per its
ownership/error rules; the unrelated browser task was untouched and no browser
upgrade was installed. This new report is uncommitted; the 21 pre-existing
untracked documents remain byte-identical. No live journey is established here.
