# v3.1.81 / 159 — approved r24 exception qualification

## Result

Implementation commit `ed759a0ccaef5d1ffd14be5a41a0e915a6268ff7` is pushed to main.
[Verify 36013271302](https://github.com/albertloky/SBXR/actions/runs/36013271302)
and [candidate 36013440147 attempt 1](https://github.com/albertloky/SBXR/actions/runs/36013440147)
both succeeded. Both native architectures passed the candidate's ordinary full
tests, race checks, vet, packaging/menu verification and scans. Preflight,
draft construction, signing and the bounded Owner-exception result succeeded.
The VPS live job and failure cleanup job were skipped.

The release remains an unpublished draft, release ID `395789410`.
Signed manifest SHA-256:
`f875bc64c0eb0d07871b3b2d1bd13823e3eddd32275463659f847f374867cb45`.
Release-index SHA-256:
`91782e8cebbb649971989883687911479798f2411e6c49055ed7f06dd277c27f`.

## Evidence and limits

The exact target-bound Git review, unsigned decision chain, four authenticated
assets, both native receipts, signed boundary and source/workflow attestation
were independently checked locally before applying the approved signing and
exception gates. The hosted exception decision reproduced byte-for-byte. Draft
notes match its Acceptance Record and the four assets remained unchanged.

Qualification explicitly reuses v3.1.80/r24 evidence, with the real late Owner
confirmation and unknown actual cleanup time. No new Karing journey, proxy
installation or CA issuance was performed. Signing retained its ordinary
read-only host-readiness check. v3.1.80 remains failed/burned; v3.1.81 is not burned.
Public Latest remains v3.1.0. No stable-publication workflow or publish action
was executed.

A fresh **read-only** stable-preflight evaluation also accepted the actual signed
candidate/result and returned proposed publication actions. Those actions were
not executed. Publication still requires separate Owner approval and fresh
normal publication checks. Public reader/finalization behavior was exercised
with real CLI/HTTP fixtures, not by publishing this draft.

## Regression validation

Local full `go test ./... -count=1`, vet, focused race tests, real temporary Git
source comparison, HTTP reader fixtures, and CLI qualification/publication-chain
fixtures passed. Negative tests cover altered history, wrong target, missing
review/approval, changed historical package declarations, failed native run,
burned target and fabricated fresh scenario evidence. Ordinary timed live
validation and the old v3.1.0 exception remain unchanged.

One earlier local full-test invocation overlapped a disclosure edit, causing a
mixed-version record comparison failure. The unchanged-tree rerun passed; no
assertion or timeout was weakened. The first local declaration preparation was
also refused before dispatch because it retained a signing-only baseline and
used noncanonical JSON encoding; its corrected input passed. Exactly one new
candidate was dispatched; no remote retry was needed.

[Local validation and independent reviews](../../../.scratch/acceptance/i24/).
This report is retained locally, not included in the already-qualified source
commit; adding a report must not silently change the publication source binding.
