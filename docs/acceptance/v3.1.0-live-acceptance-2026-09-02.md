# v3.1.0 post-release live acceptance: blocked

## Scope and identity

Albert requested live acceptance excluding the multi-day Certbot test. Natural timer firing and naturally due certificate renewal were not tested. This was a post-release test of unchanged public stable bytes, not a new signed qualification attempt. It does not replace the Owner-exception Acceptance Record or change the published release.

- Repository: `albertloky/SBXR`
- Tag / Release Sequence: `v3.1.0` / `83`
- Commit: `c0667a12ea914f2d0c86d73d52bfb8b40fea054a`
- Release index SHA-256: `5e9b25cf2bd5b448c0a833b6420e165bd47a207144bb63330a62e0b9dafc3cd1`
- Installer SHA-256: `d40106fdbc4ac5044bb82df716d97b87445c28479e1e69859adc67796abbc7d0`
- Approved disposable host: `RackNerd`, Ubuntu `24.04`, `x86_64`
- Actual pre-existing packages: Certbot `5.8.0`, revision `5893`; snapd `2.76.2`, revision `27710`; core24 `20260410`, revision `1643`.

The actual Certbot version differs from the earlier signed attempt's planned packages. These observations are not evidence for that attempt. Karing was not exercised.

## Observed checks

Before installation, GitHub reported no queued or in-progress runs. SBXR, its authority, proxy configuration, and qualification transport were absent. All four inspected APT/dpkg locks were available, no GitHub hosts override was present, and SSH was active.

At `2026-09-02T01:21:26Z`, the digest-checked public installer began installation. It returned `SOFTWARE-LIFECYCLE-INSTALL-INSTALLED` for `v3.1.0`. The Installed Record matched the expected commit, sequence and architecture; the executable SHA-256 matched that record. The packaged menu reported `Not set up`, `Not enabled`, and Software Lifecycle `Ready`.

Declining Start setup returned `PROXY-INSTALLATION-ACTION-CANCELLED`. Executable and Installed Record digests were unchanged, and no Ownership Record appeared.

Confirmed Start setup ran from `2026-09-02T01:21:52Z` to the post-check at `2026-09-02T01:22:15Z`. It returned `PROXY-INSTALLATION-SETUP-COMPLETE`; the menu reported `Running` / `Not enabled`. `sing-box.service` was active and enabled, and the Ownership Record was mode `0600`. These are local setup observations, not outside-client traffic proof.

## First unexpected failure

The Running menu offered neither Enable subscription nor Rotate Client Identity. View details reported:

`Subscription enablement check: Shared Certbot admission`

A repeated read-only check of the unchanged packaged menu failed the assertion that `Enable subscription` was present. Test mutations stopped at this failure; only read-only diagnosis and separate safety cleanup followed.

Diagnosis found:

- `pgrep --exact certbot` returned `1`: no matching process.
- `snap.certbot.renew.service` was inactive/dead, with `MainPID=0` and `Result=success`; its timer was waiting.
- No Certbot/Let's Encrypt/SBXR locks appeared in the `lslocks` observation.
- All three `.certbot.lock` files were absent.
- `/var/log` was a directory owned by UID `0`, GID `104` (`root:syslog`), mode `0775`.
- `/var/log/letsencrypt` was root-owned mode `0700`; the other inspected ancestors were root-owned mode `0755`.

The packaged source's `PreflightSubscription` calls `safeParents` for `/var/log/letsencrypt/.certbot.lock`. `safeParents` rejects group- or other-writable ancestors. `/var/log` therefore makes `RenewalIdle.Accepted` false even though the process/lock observations are idle. `subscriptionAdmission` renders that failure as Shared Certbot admission with a wait-for-work correction. The same false admission removes Rotate Client Identity from the menu. Waiting for Certbot does not resolve the observed parent-permission condition.

No directory permissions, safety check, package version, or executable bytes were changed to bypass this refusal. No certificate issuance, subscription enablement, or client credential disclosure was performed. The Certbot version change is an observed environment difference, not the demonstrated cause of this refusal.

## Separate safety cleanup

After reviewing Complete removal, the exact `REMOVE SBXR` confirmation ran from `2026-09-02T01:24:17Z` to the post-check at `2026-09-02T01:24:30Z`. The unchanged package returned `SOFTWARE-LIFECYCLE-COMPLETE-REMOVAL-COMPLETED` and `SBXR is not installed.`

Fresh checks proved absence of `/usr/local/bin/sbxr`, `/var/lib/sbxr`, `/var/lib/.sbxr-removal.json`, `/etc/sing-box`, `/var/lib/sing-box`, the SagerNet APT source/key, and the installed sing-box package. The downloaded installer and its empty temporary directory were removed. SSH remained active. Pre-existing Certbot/snap packages and shared directories were preserved. Cleanup success does not change the blocked acceptance result.

## Remaining evidence

Subscription enablement, live trusted HTTPS, link and Client Identity rotation, outside traffic, interruption/reboot recovery, shared renewal/recorder scenarios, and the complete final Karing journey remain unperformed. No full secret-capture coverage or client-compatibility success is claimed. Incoming source upgrades remain not applicable to this clean-install-only release. Natural renewal remains not observed by request.

The separately documented production issuance budget also prevents the complete repeated fresh-enablement/removal matrix from fitting one session on this address. This run stopped earlier, at admission, without consuming an issuance request. No signed scenario pass, successful Acceptance Record, release burn, or release-state change was produced.

## Approved host correction and bounded recheck

Albert subsequently approved changing only `/var/log` from `0775` to `0755`, plus code/test fixes for the misleading diagnosis. The original failed acceptance above is preserved; this section records a separate bounded recheck, not resumed signed qualification.

At `2026-09-02T01:49:18Z`, fresh checks found no queued/running GitHub jobs, no SBXR installation or qualification transport, and the same `/var/log` UID `0`, GID `104`, mode `0775`. Before changing permissions, a unique non-secret `logger` marker reached both the journal and `/var/log/syslog`; `rsyslogd -N1` and `logrotate --debug /etc/logrotate.conf` succeeded.

By `2026-09-02T01:49:37Z`, `/var/log` was mode `0755` with unchanged owner, group, device and inode. `/var/log/letsencrypt` remained root-owned mode `0700`. Rsyslog remained active, another marker reached both logging destinations, and both configuration checks passed. No actual log rotation or future logging workload was exercised. No child ownership/permission, ACL, package, or service configuration was edited.

From `2026-09-02T01:50:04Z` to `2026-09-02T01:50:21Z`, unchanged public `v3.1.0` was clean-installed and set up through its packaged menu. Both `Enable subscription` and `Rotate Client Identity` were then offered. Its executable still matched the Installed Record. This directly verifies that the approved host correction removes the observed action-admission blocker without a code or safety-policy bypass.

Both actions were reviewed and declined. Each returned `PROXY-INSTALLATION-ACTION-CANCELLED`; executable, Installed Record, Ownership Record, and proxy-configuration digests were unchanged. No subscription certificate or credential was created or disclosed. Complete removal then ran from `2026-09-02T01:51:00Z` to the post-check at `2026-09-02T01:51:16Z`, returning `SOFTWARE-LIFECYCLE-COMPLETE-REMOVAL-COMPLETED`. The checked executable, authority, proxy state/configuration and SagerNet APT source/key were absent. SSH and rsyslog remained active; `/var/log` retained the approved `0755` mode.

This proves action availability and cancelled reviews on the corrected host. It does not prove actual enablement, identity rotation, outside traffic, Karing, certificate renewal, or the forthcoming code-diagnostic changes in a released package.

## Local diagnostic fix and verification

The separate `codex/certbot-admission-diagnostics` source change preserves strict parent-directory and Certbot lock safety. It reports the logical unsafe path and mode, distinguishes busy work from unsafe or uninspectable facts, and explains both unavailable actions through Review, the numbered menu, and changed-fact Execute refusal. It does not change host permissions automatically or make a refused action selectable.

Regression checks cover the real `0775` ancestor/`0700` child layout, read-only refusal, acceptance after `0755`, symlinks, hard links, unsafe lock modes, unknown inspection, a child-held POSIX lock, both typed menu explanations, and the already-enabled subscription case. Focused tests passed with Go `1.26.6` on `darwin/arm64`. Independent spec and code-quality reviews passed after adding direct assertions for the typed unavailable-action data.

The first simultaneous full and race runs both failed the unchanged release-workflow missing-event test's `15*time.Second` guard and were stopped. The same subcase failed three isolated repeats. A matching 100-iteration `grep`/`sleep .01` loop took `17.008` seconds on this machine. The test-only loop was reduced to three repeated missing observations; production polling, the test deadline, forced-death checks, and cleanup assertions are unchanged. All three complete interruption-test repeats then passed. This is a test-runner correction, not packaged interruption or recovery evidence.

The sequential full run then passed every package except `cmd/sbxr-release`, which reached Go's cumulative ten-minute package-test limit. Its active test had run for eleven seconds when that limit fired. The retry uses a thirty-minute package-runner limit, without changing production deadlines or individual safety-test guards. Interrupted or timed-out runs are not counted as passes.

Final local verification on the unchanged diagnostic-fix source:

- Every package passed non-race testing: `GOTOOLCHAIN=go1.26.6 go test -p 1 ./... -count=1` supplied the other package results; `GOTOOLCHAIN=go1.26.6 go test -timeout 30m ./cmd/sbxr-release -count=1` passed the timed-out package in `477.020s`.
- `GOTOOLCHAIN=go1.26.6 go test -race -p 1 -timeout 30m ./... -count=1` passed in full.
- `GOTOOLCHAIN=go1.26.6 go vet ./...` and `GOTOOLCHAIN=go1.26.6 go mod verify` passed.
- `GOTOOLCHAIN=go1.26.6 GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build ./...` passed. This is a cross-build, not execution of the new source on Ubuntu.
- Formatting and `git diff --check` passed.

These local results do not change the public `v3.1.0` bytes or Acceptance Record. The diagnostic fix is not installed on the VPS or published as a release. The original full live-acceptance gaps remain open.

## Remaining-live-acceptance retry: 2026-09-02T02:40Z

Albert requested the remaining live acceptance again, excluding the multi-day test. This was another post-release check of unchanged public `v3.1.0`, not a signed qualification attempt or a test of local commit `adef3db`. Natural timer firing and naturally due renewal remained excluded and unobserved.

Fresh preflight beginning at `2026-09-02T02:40:04Z` found no queued/running GitHub acceptance jobs, no SBXR installation or qualification transport, idle package locks, no GitHub hosts override, and locally free TCP `80`, `443`, and `8443`. Local bind checks do not prove external reachability. `/var/log` retained mode `0755`, UID `0`, GID `104`; `/var/log/letsencrypt` remained root-owned mode `0700`. SSH and rsyslog were active. Certbot `5.8.0` revision `5893`, snapd `2.76.2` revision `27710`, and core24 `20260410` revision `1643` were unchanged from the preceding bounded recheck.

### Observed result and stop boundary

The pinned installer began at `2026-09-02T02:41:42Z`. Its digest matched the identity above; the installed executable matched its Installed Record, tag, commit, sequence, and index digest. Setup through the zero-argument menu completed by `2026-09-02T02:42:04Z` with `PROXY-INSTALLATION-SETUP-COMPLETE`, `Running` / `Not enabled`, and both previously missing actions visible.

Confirmed Enable subscription ran from `2026-09-02T02:42:29Z` to `2026-09-02T02:42:34Z`. It returned `PROXY-INSTALLATION-ACTION-REFUSED`, failed check `SBXR mutation lock`, and correction `Wait for active SBXR work and restore the existing safe mutation lock before reviewing again.` The menu process exited `0`; the acceptance assertion failed because the action was not enabled. No subscription progress phase or certificate request began. Test mutations stopped at this unexpected refusal.

Read-only diagnosis at `2026-09-02T02:43:02Z` established:

- `/run` was root-owned mode `0755`; `/run/lock` was root-owned mode `1777`.
- `/run/lock/sbxr.lock` was a root-owned, one-link, regular empty file, mode `0600`.
- No SBXR or Certbot process was observed. A nonblocking exclusive `flock` on the existing lock succeeded and was immediately released.
- Ownership remained schema `1`, phase `Running`, with no subscription enablement, serving, rotation, or repair authority. The subscription credential, dedicated renewal configuration, and lineage were absent. Proxy, SSH, and rsyslog services remained active.
- The executable still matched its Installed Record during the subsequent inspection.

The source cause is `AcquireSubscriptionReviewLock` in `internal/proxyinstallation/adapter/host/subscription.go`: it calls the generic `safeParents` before the existing-file mutation-lock helper. `safeParents` rejects group/other-writable parents, including this `/run/lock` mode `1777`. Thus the attempt refuses before trying to acquire the idle protected lock. Setup and removal use the different `AcquireMutationLock` path. The same subscription review-lock helper is also called by private Subscription Serving startup and renewal recording; those sibling routes were inspected statically, not exercised live. Its Host test creates a mode `0755` lock directory and does not cover this host layout. The relevant helper is unchanged in the local Certbot-diagnostic fix, so that unreleased fix does not resolve this new blocker.

No `/run/lock` permission change, lock-file replacement, check bypass, or product-code fix was performed. A correction must preserve the shared lock directory and strict file/ownership/link/inode/exclusion checks; it needs separate implementation and verification before acceptance can continue.

### Separate cleanup and remaining evidence

Supported Complete removal ran from `2026-09-02T02:44:07Z` to the post-check at `2026-09-02T02:44:24Z`, with exact `REMOVE SBXR` confirmation. It returned `SOFTWARE-LIFECYCLE-COMPLETE-REMOVAL-COMPLETED`, `SBXR is not installed.`, and exit `0`. The executable, Installed Record/Ownership Record directory, finalization authority, proxy configuration/state, and SagerNet APT source/key were absent. The temporary installer and its empty directory had already been removed. SSH and rsyslog were active; the pre-existing snap packages remained. `/run/lock` retained `1777`; `/var/log` retained the approved `0755` mode.

| Remaining acceptance area | Result in this retry |
|---|---|
| Subscription enablement | Attempted; refused before mutation by the shared mutation-lock path. |
| Trusted outside HTTPS and subscription request behavior | Not reached. |
| Link and Client Identity rotation, outside sessions and revocation | Not run after the unexpected refusal. |
| Repair, managed renewal/recorder, shared-route and interruption/reboot cases | Not run after the unexpected refusal. |
| Lifecycle menu operations and complete pending-operation removal matrix | Not run; only separate safety cleanup passed. |
| Final exact Karing journey | Not reached; app/profiles/settings unchanged. |
| Natural timer firing and naturally due renewal | Excluded by request; not observed. |

Fresh official Karing Latest metadata identifies `v1.2.24.2709`, asset `karing_1.2.24.2709_macos_universal.dmg`, size `103103114`, SHA-256 `7251c079316078a82857785e59b5118df5ae4242898acbc05af57f92cdd5d6e1`. Installed app metadata remains `1.2.23`, build `2606`. Updating the app was requested for approval but not performed; exact latest-package client acceptance remains open.

No Subscription Link or Client Configuration was disclosed. The initial setup output included its public endpoint; this retry does not claim complete capture-policy compliance or full secret-capture coverage. Later menu output was restricted to status, phase, result, and refusal fields. No successful Acceptance Record, signed scenario pass, candidate burn, or release change was produced. Cleanup does not turn the failed acceptance into a pass.

## Approved lock correction: source and Linux mechanics only

The approved follow-up fixes the shared `AcquireSubscriptionReviewLock` admission
used by subscription enablement, Subscription Serving startup, and renewal
recording. Only `/run/lock/sbxr.lock` admits an expected-owner real sticky parent;
its ancestors and all other paths retain strict parent checks. No shared directory
permissions are changed. The existing lock still must be a single-link regular
0600 file owned by the expected user. Read-only acquisition never creates it.

Independent review found that an attacker-created FIFO at the shared lock path
could block the read-only open before type validation. Existing-lock acquisition
now uses `O_NONBLOCK` so the existing regular-file check refuses it promptly.
Both the original sticky-parent regression and this bounded FIFO regression were
observed failing before their respective fixes and passing afterward. Follow-up
review found no remaining critical or important issues.

At `2026-09-02T02:59:56Z`, fresh read-only preflight found no queued/in-progress
GitHub runs, no installed SBXR, no `/run/sbxr-qualification`, and no listed SBXR,
Certbot, APT or dpkg process. `/run/lock` remained root:root 1777. A cross-compiled
Linux amd64 Host Adapter test binary, SHA-256
`8fa4e75586544941ac8d9aa3b407cec5411c461441e130e4efbe0334a7643450`, passed
`TestSubscriptionReviewLock` and all its subtests as root on the acceptance VPS.
This includes foreign-owner refusal, which the non-root Mac run skips.

The first test location under `/run` could not execute because the mount has
`noexec`; no mount option was changed. The test ran from a fresh root-only
directory under `/root`. Both temporary binaries and their directories were
removed. At `2026-09-02T03:03:07Z`, cleanup checks confirmed SBXR executable/state
and qualification path remained absent, and `/run/lock` remained root:root 1777.
The tests used temporary fixtures only, not product authority or a replacement
installed executable. These are Linux lock-mechanics results, not packaged live
acceptance or outside-client evidence.

The public release remains immutable `v3.1.0` at
`c0667a12ea914f2d0c86d73d52bfb8b40fea054a`. No candidate, stable release, or tracker
state was changed. Packaged acceptance of corrected bytes remains blocked:
`validSubscriptionHistory` refuses another `first-subscription-clean-install`
after the published subscription release, while `newQualificationGateway` admits
version-3 transport only for `v3-subscription-clean`, not `v3-recurring`. Extending
that existing delivery route requires a separate approved change; neither
relabeling local bytes as `v3.1.0` nor changing protected records is an acceptable
substitute. All previously unperformed live scenarios remain unperformed. The
natural multi-day Certbot test remains excluded as directed.

Final source verification used `GOTOOLCHAIN=go1.26.6` and passed:

- `go test -p 1 -timeout 30m ./... -count=1` after the final FIFO fix.
- `go test -race -p 1 -timeout 30m ./internal/proxyinstallation/... ./internal/softwarelifecycle/... -count=1`.
- `go vet ./...` and `go mod verify`.
- `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build ./...` and the equivalent arm64 build.
- `git diff --check` and independent code review after the FIFO correction.

These checks do not change the incomplete packaged live-acceptance result.
