# v3.1.68 diagnostic and consolidated repair — 2026-09-12

This was one diagnostic candidate, not release acceptance. Candidate `v3.1.68`,
sequence 147, used commit `4e2a9a68dd13cca595b7e0b1ee2994049e15eebb` on
`107.175.53.219` in [workflow 34688295764](https://github.com/albertloky/SBXR/actions/runs/34688295764).
Implementation and harness bytes stayed unchanged during live collection.
The workflow failed and retained the candidate as a failed prerelease. No stable
release or replacement candidate was created.

Work began at 10:15:49 UTC. Diagnostics and VPS restoration finished at
11:22:48 UTC, within the 90-minute collection limit. The single repair phase
then began with a 12:37:49 UTC cutoff. Validation and final VPS verification
passed by 11:29:49 UTC.

## Qualification and separate diagnostics

The unchanged collector accepted scenarios 01–06: clean installation/removal,
foreign-file refusal, precommit cleanup, postcommit recovery, drift refusal,
and interrupted removal recovery. Real outside traffic, process interruption,
descendant cleanup, secret scans, and evidence submission passed where required.

Scenario 07 performed supported setup, all five identity startup observations,
identity rotation, outside old-connection closure, refusal of a fresh old-identity
connection, replacement traffic, and Complete removal successfully. Its evidence
assembly failed. That original failure was submitted and the collector stopped;
scenario 07 is **failed**, and scenarios 08–25 are **blocked for acceptance**.

After verifying product and transport cleanup, separate public-menu diagnostics
used the preserved candidate and original transport assets. They did not create
collector requests or count as accepted scenarios:

- Schema-1 setup and subscription enablement passed; proxy PID, configuration
  bytes, and configuration inode remained unchanged.
- The Mac fetched the subscription using physical `en0`, trusted TLS, and the
  existing artifact checker; the advertised fields matched.
- The first link-rotation command refused before selection with `label-missing`.
  A fresh inspection showed the action available. Its one targeted reproduction
  passed; the old URL returned HTTP 404 and the new URL passed the outside TLS
  artifact check. The original refusal remains unresolved, not a pass.
- Lifecycle Check, Update, and Recover passed as non-mutating operations with
  unchanged protected inventory. Complete removal passed.

Managed renewal, recorder/lock scenarios, package refresh, injected route faults,
interrupted link/identity transitions, runtime subscription repair, complete
scenario-24 coverage, and Karing acceptance remain untested in this attempt.
Karing's existing connection and settings were untouched; no assumed behavior
was used as evidence.

## Findings and repair

| Classification | Finding | Disposition |
|---|---|---|
| Command mistake; procedure omission | Scenario 07's two setup-origin reviews were recorded at 11:10:06.962190, after rotation started at 11:07:36.564817. The timing validator correctly rejected the impossible interval. | Document the pre-rotation reviews and the equivalent pre-enablement review in scenario 08. Clarify that final proof order is not review execution order. Preserve the original failure; no timestamp rewriting. |
| Harness defect | `run_action` retained the initial menu only in shell-local `LAST_ACTION_OUTPUT`, but its external failure filter printed only post-selection lines. A missing-label refusal therefore lost the available-action context when a strict caller exited. | Preserve an allowlisted, already-scanned initial menu on the existing failure output when no post-selection refusal details exist. Keep return status, success silence, secret rejection, and full retained output unchanged. |
| Unresolved cause | The independent rotation action was absent from the original menu but present on subsequent inspection. The one reproduction passed. | No product change guessed. The missing original frame cannot be reconstructed; the harness repair improves diagnosis if this recurs. |
| Command mistake | The first locally constructed failure envelope escaped Unicode in the canonical boundary JSON and was refused before submission. | Preserve that refusal and submit correctly encoded UTF-8 through the unchanged submission helper. No implementation change required. |

No product defect was confirmed. Caller review covered the `action` alias,
strict entry scripts, scenario 08's successful link extraction, and failure
cleanup's existing full-output retention. Link and later-scenario procedures
already specify their pre-action and interruption/recovery observation phases.

## Validation

- The missing-label regression failed against the original wrapper for both
  interfaces, then passed after the fix. Five real-menu subprocess tests cover
  failure context, no selection/confirmation, process cleanup, secret rejection,
  quiet success, and existing post-selection refusal details.
- Eleven timing tests passed, including the actual late-review interval and the
  corresponding scenario-08 constraint. Timing validation was not weakened.
- `TestPackagedActionReadsItsResultNotLifecycleStatus`, shell syntax, and diff
  checks passed.
- One Ubuntu 24.04 fixture rehearsal passed all 20 groups: real kernel tracing,
  systemd/cgroups, egress controls, locks, route/firewall operations, snap
  interpreter boundaries, and menu interruption/cleanup. It ran 307 helper
  tests with one skip and the existing entry rehearsal. It installed no SBXR
  and requested no certificates.
- `check-readiness.py` verified that report and its original logs against the
  final sources, then passed the local entry rehearsal and 307 helper tests
  with 18 platform skips: `ready: true`, `live_evidence: false`.
- Unchanged native build results from this candidate were reused. Earlier valid
  real-SSH command/stdin tests (11 cases) and forced-parent-death cleanup were
  reused for unchanged paths. This run also exercised actual SSH staging,
  outside disclosure pipes, six accepted submissions, and failure submission.

## Cleanup and evidence

At 11:29:49 UTC, the final host check verified no installed SBXR, owned proxy
state, subscription certificate lineage, test transport, staging directories,
test listeners, units, processes, cgroups, or firewall rules. Fixture cleanup
completed separately. Certbot revision 5893, snapd 27738, and core24 2124 were
restored; temporary snap holds were removed; APT and official renewal timers
were active/enabled. The diagnostic enablement made exactly one new issuance at
11:15:10 UTC, bringing the historical total from six to seven.

Original collector failure, per-scenario receipts, protected raw VPS archives,
independent diagnostic captures, rehearsal report/logs, and cleanup receipts are
retained privately under
`/Users/Albert/.codex/sbxr-diagnostic-4e2a9a6-k2gicknv`.

The repaired harness is regression-tested and fixture-validated. One fresh
qualification is justified after explicit instruction, with the observation
phases followed and actual Karing assistance or automation available. The
unresolved menu refusal remains a risk to investigate if it recurs. This report
does not establish release readiness or authorize another candidate.
