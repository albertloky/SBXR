# Cloudflare Tunnel acceptance

This record covers automated Cloudflare authority, management-token lifecycle, installation, managed repair, owner-assisted run-token rotation, and Complete-removal transaction checks. Real destructive Cloudflare behavior, Owner Acceptance, and Release Qualification remain Pending.

## Stable checks

- `CLOUDFLARE-MODULE`: `go test ./internal/cloudflaretunnel`
- `CLOUDFLARE-AUTHORITY`: `go test ./internal/cloudflaretunnel -run 'TestView(VerifiesOneScopedCloudflareAuthority|FailsClosedWithoutLeakingAuthority)'`
- `CLOUDFLARE-SEAM`: `go test ./internal/cloudflaretunnel -run 'TestHTTPAPI(ParsesOfficialShapesWithScopedAuthenticationAndPagination|RefusesMalformedAmbiguousAndUnsafeResponses)'`
- `CLOUDFLARE-PLAN`: `go test ./internal/cloudflaretunnel -run 'Test(Plan|Executor|WholeTunnel|CloudflaredService|InstalledCloudflared|LocalOrigin)'`
- `CLOUDFLARE-SERVICE-SEAM`: `go test ./internal/cloudflaretunnel -run 'TestExecutorInstallsAndRollsBackProtectedCloudflaredService|TestLocalOriginObserverRequiresHTTP'`
- `CLOUDFLARE-ROUTE-SEAM`: `go test ./internal/cloudflaretunnel -run 'TestHTTPMutationAPI(ConfiguresAndObservesBothIndependentRoutes|RefusesContradictoryWholeTunnelIdentifiers)'`
- `CLOUDFLARE-DIRECT-DNS`: `go test ./internal/cloudflaretunnel -run 'TestPlan(PublishesDirectDNSOnlyForQualifiedAddressFamilies|RefusesUnqualifiedAddressesAndHostnames)'`
- `CLOUDFLARE-CERTIFICATE-DNS`: `go test ./internal/cloudflaretunnel -run 'Test(HTTPMutationAPI(ExposesSecretFreeAuthoritativeDNSAndEffectiveCAA|ClassifiesDNSPropagationAndDrift|ConsumesEveryEffectiveCAAPage)|ExecutorExposesCertificateDNSFactsWithoutProviderAuthority)'`
- `CLOUDFLARE-XHTTP-ROUTE`: `go test ./internal/cloudflaretunnel -run 'TestWholeTunnelHealthRequiresBothIndependentRoutesAndOrigins/xhttp|TestExecutorInstallsAndRollsBackProtectedCloudflaredService'`
- `CLOUDFLARE-WEBSOCKET-ROUTE`: `go test ./internal/cloudflaretunnel -run 'TestWholeTunnelHealthRequiresBothIndependentRoutesAndOrigins/websocket|TestExecutorInstallsAndRollsBackProtectedCloudflaredService'`
- `CLOUDFLARE-WHOLE-TUNNEL`: `go test ./internal/cloudflaretunnel -run 'Test(TunnelHealthKeepsIndependentRouteRows|WholeTunnel(HealthRequiresBothIndependentRoutesAndOrigins|WaitsForReconnectWithoutLosingEitherRoute|RetriesOnlyTemporaryProviderFailures|ConvergenceReturnsCompleteTypedFinding))'`
- `CLOUDFLARE-DEFERRED-STATE`: `go test ./internal/state -run TestDeferredCloudflareFinalizationPublishesProviderValuesInRevisionOne`
- `CLOUDFLARE-MANAGEMENT-TOKEN`: `go test ./internal/cloudflaretunnel ./internal/state -run 'Test(ViewReportsDeliberatelyRemovedManagementTokenWithoutFalseHealth|PlanReviewsManagementTokenReplacementWithoutChangingProviderAuthority|ManagementTokenPlanApplyBindsManagedLineageAndBurnsEveryOutcome|ManagementTokenRemovalUsesTheCurrentStateInventory|PrepareManagementTokenChangeAcceptsOnlyTheReviewedCloudflarePlan|PrepareManagementTokenReplacementRejectsDifferentSelectedAuthority|ManagementTokenDependencyBindingRequiresEveryExactOwner|ManagementTokenChangePublishesOnceWithoutExposingEitherToken|ManagementTokenReplacementRollbackAndRestartRestoreTheOldToken|LoadDistinguishesRemovedManagementTokenFromMissingSecret)'`
- `CLOUDFLARE-RUN-TOKEN-ROTATION`: `go test ./internal/cloudflaretunnel ./internal/state -run 'Test(PlanNamesOwnerAssistedRunTokenRotationAndBindsOwnedIdentifiers|HTTPMutationAPIRetrievesTheCurrentTunnelTokenOnlyThroughTheDocumentedEndpoint|ExecutorRemovesTheOldTokenAtCheckpointAndRestartsOnlyWithTheNewToken|PrepareRunTokenRotationKeepsTheOldTokenOnlyInRollbackMaterial|RunTokenRotationCancellationImmediatelyBeforeTheCheckpointRollsBack|OwnerAssistedRunTokenRotationPausesThenRecoversForwardWithBothRoutes|WholeTunnelHealthRequiresBothIndependentRoutesAndOrigins)'`
- `CLOUDFLARE-MANAGED-REPAIR`: `go test ./internal/cloudflaretunnel ./internal/state -run 'Test(ManagedRepair|PlanManagedRepair|HTTPMutationAPIRepairs|ManagedCloudflareRepair)'`
- `CLOUDFLARE-COMPLETE-REMOVAL`: `go test ./internal/cloudflaretunnel ./internal/systemchanges ./internal/state -run 'Test(RemovalAuthorityRefusesAChangedImmutableID|CompleteRemovalAcceptsOnlyOrderedTypedOwnedResources|CompleteRemovalDurablyCrossesIrreversibleBoundaryBeforeCloudflareDeletion|CompleteRemovalFailureAfterTheIrreversibleBoundaryResumesForward|CompleteRemovalWaitsForVerifiedRevocationBeforeDeletingTheLocalToken|UbuntuAdapterReversesOnlyTypedCompleteRemovalResourcesAfterRestart)'`
- `CLOUDFLARE-TRANSACTION`: `go test ./internal/systemchanges ./internal/systemchanges/adapter/ubuntu`
- `CLOUDFLARE-NETWORK-PATH`: `go test ./internal/networkpolicy -run TestEvaluateBoundsOutboundAndRenewalFreshness`
- `CLOUDFLARE-STATE-COMMIT`: `go test ./internal/state -run TestFreshInstallationPublishesRevisionOneOnlyAtPublication && go test ./internal/state/adapter/filesystem -run 'TestProductionPlacement|TestAtomicRollbackRestoresManagedOrNotInstalledBaseline'`
- `CLOUDFLARE-REPOSITORY`: `go test ./...`

## Procedure

1. Create a memory-only `cfat_` token fixture containing a unique marker. Confirm a Global API Key, missing Adapter, missing clock, malformed immutable ID, invalid zone name, and missing Network Policy proof stop before a provider call.
2. Return the exact selected account and zone, active token, future expiry, assigned nameservers, matching public nameservers, `Account API Tokens Read`, `Cloudflare Tunnel Edit`, and `DNS Write` policies scoped only to the selected account and zone. Confirm `CLOUDFLARE-AUTHORITY-VERIFIED` is Healthy while `WritesProven` remains false.
3. Remove each permission, add an unrelated permission, use an all-zone wildcard, add a second resource, change an immutable ID, disable the token, expire it, and change the zone binding in turn. Confirm each result fails before mutation with `CLOUDFLARE-TOKEN-PERMISSION` and exactly `Check current token again`, `Enter replacement token`, `Verify replacement`, and `Back`.
4. Fail Cloudflare HTTPS, TCP `7844`, and UDP `7844` in turn. Confirm Cloudflare Tunnel consumes the typed Network Policy result, returns `CLOUDFLARE-NETWORK-PATH`, makes no API request, and offers no network or provider-firewall mutation.
5. Return a pending or incorrectly delegated zone. Confirm `CLOUDFLARE-ZONE-PENDING` includes assigned and publicly observed nameserver facts and exactly `Check again`, `Wait another 10 minutes`, and `Back and continue later`.
6. Through controlled official-shape HTTP fixtures, confirm Bearer authentication appears only in `Authorization`; selected account and zone filters remain in every path/query; pages are consumed once and in order; exact immutable IDs survive parsing; and duplicate, malformed, contradictory, oversized, or unbounded results fail safely.
7. Return unauthorized, forbidden, rate-limited, server, malformed, false-success, DNS, and transport failures with unique raw-output markers. Confirm the Interface exposes only stable error kinds and typed Health text. Temporary failure receives at most three attempts and two 30-second waits inside 60 seconds.
8. Inspect ordinary and Go formatting of the token, requests, result, Health, policies, errors, and walkthrough. Confirm the complete token, authorization value, provider marker, raw provider output, and secret-derived hash are absent. Confirm only dedicated credential detail contains the first and last four characters.
9. Confirm a failed or pending `View` returns no verified State handoff and one Healthy `View` returns only the opaque Infrastructure Secret type accepted by State. Separately confirm the existing State/System Changes transaction keeps storage absent before publication, places its Cloudflare management token in revision `1` only after publication, keeps the filesystem path and modes State-owned, and restores the proven Not installed baseline without the token on rollback. `View` still owns no file, publication, lock, journal, Rollback Snapshot, token creation/revocation, DNS mutation, Tunnel mutation, or provider-firewall path. The complete-executable coordination remains Integrated Verification, so it stays Pending below.
10. Build the installation Plan twice from the same fixture and confirm the identity and checksum are deterministic while all rendered values omit the management and run-token markers. Change the bound token ID, account, zone, hostname observation, Desired State checksum, or cloudflared baseline and confirm Apply refuses before mutation.
11. Run the controlled deferred-finalization transaction. Confirm every returned Tunnel/DNS ID appears in a `Step completed` checkpoint before the next provider step, `Deferred State finalized` appears before `Activate prepared configuration`, and revision `1` contains each ID and the provider-issued run token exactly once. Confirm ordinary events and results contain no token marker.
12. Check a connected two-route observation, every partial route/DNS combination, wrong and public origins, disconnected state, delayed convergence, contradictory or duplicate identifiers, reconnect followed by complete route agreement, and each stopped loopback origin separately. Only the exact Tunnel, two independent routes, owned DNS, final 404, and both reachable loopback origins may be Healthy. Confirm no raw provider marker appears in a finding.
13. Stream the protected prepared Cloudflare service artifact containing both independent routes through the Ubuntu transaction Adapter. Confirm native ingress validation occurs before `systemctl enable --now`, the complete token appears only in the `0640` token file, fresh-install rollback removes only the installed unit/config/token, and an arbitrary TCP listener cannot satisfy either HTTP-origin check. Wrong owner, wider mode, unsafe parent or link, missing journaled ID, stale Plan, or contradictory finalization evidence must stop safely.
14. Keep reconnect, keepalive-relevant connected/route observations, and outside-client access as separate evidence. Controlled reconnect and route-agreement checks may pass locally; real Cloudflare reconnect, edge keepalive, and outside XHTTP/WebSocket access remain Pending unless an approved Acceptance Run performs them.
15. Build IPv4-only and IPv6-only Plans and confirm only the qualified Direct DNS family is created. Refuse malformed families and hostnames outside `xhttp.`, `ws.`, `direct.`, or the reviewed eight-hex-character suffix form. Through controlled official-shape fixtures, confirm the Certificate DNS handoff contains the committed Direct hostname, exact DNS-only addresses, and only the closest effective CAA set while omitting the token and every DNS record ID. Missing expected records remain temporary provider delay; extra, changed, proxied, duplicate, or contradictory records stop as drift. Confirm no CAA or unrelated `_acme-challenge` mutation is attempted.
16. Verify a replacement against the selected account and zone, then confirm the Plan keeps `WritesProven` false and the old stored token active until publication. Fail before publication and after restart; confirm rollback restores the old token. Publish once; confirm State contains only the protected replacement and no event or result exposes either token.
17. Review removal with no dependent behavior, then with the exact Tunnel, DNS, certificate, profile, repair, and update inventory. Confirm any missing, duplicate, unknown, or unresolved dependency refuses before mutation. Apply the one reviewed unmanaged result to all six, publish deliberate absence, and confirm View reports `Unknown` with exactly `Check now`, `Replace token`, and `Remove from SBXR`; no authority-dependent fact remains falsely Managed or Healthy.
18. Review `Rotate Tunnel run token`. Before `Irreversible run-token rotation started`, cancel or fail and confirm ordinary rollback preserves the old State and service token. Cross that checkpoint and confirm the old token and every rollback copy are removed before SBXR asks the Owner to select **Rotate token**. An unchanged documented GET result must keep waiting. A changed result must pass opaquely to State, create the new protected service artifact, restart `cloudflared.service`, and require the connected Tunnel plus both XHTTP and WebSocket routes before and after publication. Fail restart, either route, publication, or agreement and restart recovery at each durable phase; confirm Recovery Required continues only forward and never writes, starts, logs, or retains the old token. Keep the real provider rotation and forward-recovery run Pending unless separately approved and performed.
19. Review managed repair from current State. Confirm the Plan selects only the committed Tunnel and DNS identifiers, refuses missing or same-named different identifiers, refuses unreachable local origins, and is rechecked before rollback capture. Drift routes and DNS through controlled fixtures, then confirm exact provider pre-images restore after failure, temporary calls stop after three attempts inside 60 seconds, deterministic calls stop immediately, service restart follows transaction rollback, and the unchanged Desired State intent publishes as the next revision only after both routes pass. Restart at each durable phase and confirm the existing journal resumes or enters Recovery Required without `Continue anyway`. Keep real Cloudflare mutation, DNS propagation, edge HTTPS, external XHTTP/WebSocket, and live VPS restart Pending unless separately approved and performed.
20. Review Complete removal from Managed and Recovery Required. Confirm the Plan lists the exact committed DNS, route, and Tunnel identifiers, the absence gate, `Irreversible removal started`, token revocation, and `Not installed` without a token value. Change any identifier and confirm refusal before mutation. Fail or cancel during public cleanup and confirm rollback. Immediately before the first Cloudflare deletion, confirm the irreversible record is durable; then fail, cancel, and restart at each Cloudflare and later cleanup phase and confirm the journal continues only forward. Keep the local token until owned external absence is proven, ask the Owner to revoke it, accept only verified rejection, remove the local copy, and report that DNS caches cannot be erased. Keep real deletion and revocation Pending unless an exact approved Acceptance Run begins and returns to an approved Acceptance Baseline.

## Current status

| Stage | Owner | Status | Expected safe evidence |
|---|---|---|---|
| Module Verification | Codex | Passed for authority, management-token lifecycle, run-token rotation, managed repair, Complete removal, both routes, Direct DNS, and installation | `CLOUDFLARE-MODULE`, `CLOUDFLARE-AUTHORITY`, `CLOUDFLARE-MANAGEMENT-TOKEN`, `CLOUDFLARE-RUN-TOKEN-ROTATION`, `CLOUDFLARE-MANAGED-REPAIR`, `CLOUDFLARE-COMPLETE-REMOVAL`, `CLOUDFLARE-PLAN`, `CLOUDFLARE-DIRECT-DNS`, `CLOUDFLARE-CERTIFICATE-DNS`, `CLOUDFLARE-XHTTP-ROUTE`, `CLOUDFLARE-WEBSOCKET-ROUTE`, `CLOUDFLARE-WHOLE-TUNNEL`, `CLOUDFLARE-NETWORK-PATH`, and `CLOUDFLARE-STATE-COMMIT` pass with marker-safe results. Real destructive removal remains Pending. |
| Seam Verification — controlled fixture | Codex | Passed for authority, management-token replacement/removal, owner-assisted run-token rotation, managed repair, both routes, Direct DNS, and installation | Official-shape HTTP, token replacement/removal publication and rollback, documented run-token GET, irreversible checkpoint, protected token replacement, exact-ID provider repair and pre-image rollback, controlled restart, exact two-route publication, qualified Direct address families, effective CAA handoff, Plan freshness, provider steps, protected service installation and rollback, separate HTTP-origin proof, whole-Tunnel health, deferred State finalization, and restart-recovery checks pass without an Owner credential. |
| Integrated Verification | Integrated release runner | Pending — integrated release | The complete executable still needs complete removal and real service/provider coordination. |
| Codex Live Acceptance | Codex, only during an approved Acceptance Run | Pending — approved Acceptance Run | No Acceptance VPS, real Cloudflare token, provider resource, or outside client was used. |
| Owner Acceptance | Albert | Pending — first v1 release | Albert has not yet created a scoped token or accepted the onboarding walkthrough in the maintained Cloudflare dashboard. |

The implementation result is `Management-token replacement/removal, owner-assisted run-token rotation, managed repair, Complete-removal transaction behavior, both Tunnel routes, Direct DNS/CAA handoff, and whole-Tunnel health verified locally; Cloudflare integrated and Release Qualification pending`. Real provider-token replacement/removal/rotation/repair, destructive Cloudflare cleanup, Cloudflare delegation, authoritative DNS propagation, edge behavior, reconnect/keepalive, forward-only live recovery, and outside access remain Pending. Unperformed rows stay Pending.
