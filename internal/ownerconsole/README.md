# Owner Console

Owner Console owns SBXR's sighted full-screen terminal presentation behind `Run`. Running `sbxr` with no private mode detects the attached terminal, refuses unsafe sessions before drawing, resets modes it owns, enters Bubble Tea's alternate screen, and restores the terminal on every manageable exit.

The Style A frame keeps persistent navigation beside one main work area. `80×24` is the minimum admitted size. At `120×36`, the same navigation remains in place and a second column adds relevant typed details. A later resize below the minimum pauses activation, reports the current and required dimensions, and resumes the same scenario and selection after the terminal is enlarged.

Navigation uses arrows or `Tab` and `Shift+Tab`; `Enter` or `Space` selects; `Esc` goes back only when Back is legal; and `Ctrl+C` opens the visible Exit SBXR confirmation. `Q` and `q` have no exit binding. Bracketed paste is handled as paste data rather than application shortcuts. Every screen has a contextual two-line shortcut bar; forward-only removal never advertises Back or cancellation.

An active input field treats ordinary typed characters and bracketed paste as data. Confirmation words and shortcut letters do not activate anything. Control and escape-like bytes are never written back to the terminal as controls; the field marks terminal controls as neutralized, and immediate bytes after a hostile paste terminator remain safe visible data. Resize below `80×24` pauses input and activation without clearing the field, focus, navigation selection, reviewed Plan screen, operation screen, prompt, error, or result. A typed background presentation update waits while a confirmation is open, then changes facts after explicit dismissal without moving those interaction choices.

`Session.Updates` accepts only typed `PresentationUpdate` values. `OperationID` identifies continuing timed work so repeated updates preserve its elapsed clock. Measured progress requires completed and total units. Unknown-duration progress uses Owner Console's monotonic elapsed timer and no percentage. Mixed-step progress identifies `CurrentStep` out of `TotalSteps` and shows a bar only when that step has a real total. Missing, invalid, unsupported, or screen-incompatible progress is shown as unavailable; Owner Console does not infer a percentage, completion time, success, rollback, or service state.

Admission checks interactive input, interactive output, alternate-screen support, full-screen cursor addressing, readable text, standard keyboard input, and the current terminal size. It asks the attached terminal to report the required drawing modes rather than accepting a list of terminal product names. Unicode terminals receive Unicode separators; other admitted terminals receive ASCII separators. Bubble Tea and Lip Gloss use a reliably reported existing background, degrade to monochrome, and honor `NO_COLOR`; text and selection markers carry every meaning without color.

Before drawing, Owner Console records the terminal's reported screen, cursor, mouse, and paste modes, resets the modes it owns, and admits the full-screen frame only after the required drawing-mode replies arrive. A manageable exit restores the prior keyboard settings and every reported owned mode, including modes that were enabled before launch.

The approved Style A scenarios are typed presentation fixtures. They cover Overview, dedicated Access, installation review, Cloudflare guidance, Correction Flow, truthful progress, cancellation, both Recovery Required branches, update review, both Complete-removal phases, Live Profile Check, and undersized pause at both approved sizes. They do not perform product work. Outcome Modules remain responsible for State, release verification, profiles, subscriptions, Cloudflare, certificates, diagnostics, Plans, mutation, rollback, recovery, and privileged operations.

`Session.Outcome` is the reusable typed seam for one consequential Owner request. Owner Console asks the owning Module for a complete secret-safe review, passes only the exact typed Plan identity after ordinary system authentication, presents a fresh Plan after stale or reused refusal, and sends explicit Fix, Check again, and cancellation requests back to that owner. Closing the Console before approval starts nothing. After approval, the operation request uses the owning Module's independent operation context, so terminal loss is not a cancellation request. Relaunch uses only typed durable Change Set facts: active progress, proven success, proven rollback, or Recovery Required. It never treats process exit or service reachability as success.

Plan and Correction Flow presentation rejects incomplete facts, control characters, oversized fields, invalid identities, and invalid SHA-256 values. A complete Plan shows its Desired State revision and checksum, relevant checksums, fresh Observed State, verified external inputs, exact effects, Required and Advisory checks, interruption, cancellation, and rollback. A Correction Flow shows Problem, Found, Required, why SBXR stopped, separately reviewable `Fix with SBXR`, exact Owner steps, required input or selection, Check again, Back, and copyable redacted evidence. No raw output, secret, bypass, force, adoption, or Continue-anyway authority crosses this seam.

Production starts Owner Console only after the Owner Launch Identity is root or a non-root launch has completed system authentication. The complete Console process runs as root. Owner Console owns no launch authentication or pre-authentication screen. Authentication refusal occurs before Owner Console exists. Its privacy choice controls whether Client Access Values can render in the already authenticated root Console. Installation review and Apply remain inside that same root process.

Authenticated Access accepts only typed six-profile share URIs and the universal, v2rayN, Shadowrocket, Karing, Mihomo, and sing-box links. It reports actual counts, omissions, and candidate-only Shadowrocket Owner Acceptance facts. The selected value is underlined and labeled exactly `Click or press Enter to copy`. Clipboard confirmation, unconfirmed transfer, and failure use fixed named wording and retain the visible text for manual selection. QR output is generated from that same selected value and is omitted when it cannot fit. Client Access Values are not emitted during terminal refusal, before the launch privacy choice, after the Owner continues without them, or on ordinary Overview. Infrastructure Secrets, arbitrary command output, provider responses, generated configurations, journals, and Rollback Snapshots never belong in this Module.

`Session.Profiles` is the small typed presentation seam for the six Connection Profiles and Subscription. `Session.ProfileOutcomes` is the separate Change Set seam supplied by that same owning Module. Each profile shows its enabled state, service and listener health, public address or hostname, selected port and transport, settings, exposure, and publication state. A disabled profile keeps its settings and credential while showing closed exposure and omitted publication; deliberate disablement is not called a failure. Open in Access, rotate one credential, reviewed port change, native validation, repair, enable, and disable all return to the owning Profiles Module. Consequential requests use the same exact one-use Plan and durable Change Set path as every other Owner action.

Subscription shows named representation counts and exact omissions without exposing a Client Access Value. Rotate all six profile credentials, rotate only the subscription token, and Revoke all client access are separate reviewed Change Sets. Live Profile Check is available only after successful authentication on a Managed installation. The owning Module starts it once from the universal subscription and streams one temporary URL followed by typed per-profile checked results. While the check is active, Owner Console keeps the URL or same-source QR visible with a spinner, monotonic elapsed time, and automatic pending or authenticated uplink and downlink facts for all six profiles. Owner Console stores these facts only in the current process, cancels and clears them on Back, and never treats the optional check or a manual Owner claim as proof of Managed state.

`Session.Cloudflare` presents the Cloudflare Tunnel Module's typed walkthrough, credential status, missing-permission facts, and pending-zone facts. The walkthrough fixes the current `My Profile > API Tokens`, selected-zone `DNS > Records`, and Cloudflare One `Networks > Tunnels & Mesh` paths, the selected account and zone scopes, and only the minimum Tunnel and DNS permissions; Global API Keys, broad authority, and `API Tokens Write` are rejected. Token input and replacement stay masked and memory-only. The credential view shows only status, first and last four characters, binding, last verification, optional expiry, and current uses. Its actions are Check now, Replace token, Remove from SBXR, and the separately named genuine Tunnel run-token rotation. A running check, token verification, or 10-minute wait shows a spinner and monotonic elapsed time, prevents another action, permits only safe Back or Exit, and holds a completed result behind Exit confirmation until Stay. Removal dependency refusal remains a Correction Flow and cannot present an applicable Plan while Tunnel, DNS, certificate, or profile facts would remain falsely Managed or Healthy. Provider writes still require the owning Module's exact reviewed Plan through `Session.CloudflareOutcomes`; Owner Console has no Cloudflare API or token-management authority.

`Session.Certificates` presents the Certificate Lifecycle Module's typed IP and Direct TLS lineages, fixed `shortlived` and `tlsserver` profiles, and typed activation or rollback outcome. It shows the standing policy's IP due point at `72 hours`, ordinary `6 hours` retry, busy-lock retry within `1 hour` or `15 minutes` below `24 hours`, and the domain ACME Renewal Information branch with a `15 days` fallback. Exactly one persistent randomized scheduler evaluates both lineages serially under the global mutation lock. Issuance and renewal remain separate reviewed actions through `Session.CertificateOutcomes`. Certificate Lifecycle receives typed DNS, CAA, HTTP-01, and health facts but no Cloudflare token. The Console offers no Cloudflare DNS-01, CAA creation, private-key reveal, ACME account access, issuer command, or certificate mutation path. Long provider and certificate facts are divided into Enter-driven sections at both approved terminal sizes while reserving their final actions; no PgDn action exists.

`Session.Diagnostics` keeps installation status separate from typed Module and service health. It shows stable finding codes, explanations, next actions, the fixed `30 days` or `50 MiB` event retention, and at most three completed support bundles. A fourth bundle requires choosing the exact completed archive to replace. The external-copy warning remains visible because copied or moved bundles leave SBXR's retention, deletion, permission, and redaction control. `Session.Lifecycle` presents freshly verified installed and candidate Release Identities, authenticated sequence, migrations, regenerated representations, affected services, checks, interruption, cancellation, and rollback; discovery cannot Apply, and a downgrade must be compatible with the current proven Desired State. `Session.Recovery` exposes retry only for valid unfinished rollback material, forward repair only from current proven State, and otherwise only safe evidence, diagnostics, Check again, Complete removal, and rebuild guidance. No history browser, old-secret restore, Recovery Point, adoption, force-start, force-unlock, manual completion, or parcel crosses these seams.

`Session.CompleteRemoval` presents the Software Lifecycle Module's current typed removal state and streams monotonic forward-only updates. The review names every owned local and Cloudflare category, the Certificate Transparency and DNS-cache remnants that cannot be erased, copied client configurations that may remain, the virtual VPS physical-erasure limit, Albert's scoped-token revocation responsibility, and the exact `Irreversible removal started` boundary. Owner Console forms `CompleteRemovalApproval` only after the exact `COMPLETE REMOVAL` field and a separate selected `Permanently remove SBXR` action; `Session.CompleteRemovalOutcomes` then owns the exact one-use Plan and Apply. Before the checkpoint, Back and Cancel remain available and cancellation must return the same proven Managed or Recovery Required starting status, except that a Recovery Required start may return to Managed only with the typed proof of its exact prior Managed baseline. After the checkpoint, the screen is forward-only, gives the exact Cloudflare dashboard revocation step, shows revocation verification and local-token deletion, and says that restart continues durable deletion. Success is only typed Not installed with no SBXR recovery material. No retained-recovery uninstall, backup, Recovery Point, or post-Complete restore path exists.

A manageable exit restores the terminal. Forced termination such as `kill -9`, VPS power loss, or a broken SSH client can prevent that cleanup. After a forced termination that prevented restoration, run the terminal's standard recovery command:

```sh
reset
```
